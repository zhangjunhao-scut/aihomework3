# 大模型接口调用工具箱 使用文档

> 版本：v1.0
> 日期：2026-09-10
> 形式：网页版应用（前后端分离，多用户）
> 定位：大模型 API 的「专门定制版 Postman」

---

## 目录

- [1. 项目简介](#1-项目简介)
- [2. 技术栈](#2-技术栈)
- [3. 项目结构](#3-项目结构)
- [4. 环境要求](#4-环境要求)
- [5. 启动方式](#5-启动方式)
- [6. 测试账号](#6-测试账号)
- [7. 使用指南](#7-使用指南)
- [8. 验收项](#8-验收项)
- [9. 后端 API 速查](#9-后端-api-速查)
- [10. 安全说明](#10-安全说明)
- [11. 扩展厂商](#11-扩展厂商)
- [12. 常见问题](#12-常见问题)

---

## 1. 项目简介

**大模型接口调用工具箱** 是一个用于 **测试、调试、调用各家大模型 API 接口** 的网页版工具。核心目标是让开发人员：快速构造请求参数 → 发给大模型服务商接口 → 查看原始返回结果 → 验证接口可用性与参数效果。

类比理解：**Postman / Apifox 是通用 HTTP 调试工具，本工具是专门针对大模型 API 定制版 Postman。**

核心能力：
- 多厂商切换（首版内置 DeepSeek，架构预留扩展）
- API Key 加密存储与掩码展示
- 模型选择 / 参数面板（temperature、top_p、max_tokens、stream）
- 消息编辑器（system / user / assistant 多轮对话）
- 非流式 + 流式（SSE）两种发送模式
- 结果可视化 + 原始 JSON 双视图
- 历史记录（按用户隔离，可回填）
- 一键复制 cURL / 请求 JSON

---

## 2. 技术栈

| 层 | 技术 |
|---|---|
| 前端框架 | React 18 + Vite 6 + TypeScript |
| UI 组件 | MUI v5（Material Design 风格） |
| 状态管理 | Zustand |
| 路由 | React Router v6 |
| 后端框架 | NestJS 10 + TypeScript |
| ORM | Prisma 5 |
| 数据库 | SQLite（文件型，零配置） |
| 认证 | JWT（@nestjs/jwt，7 天有效） |
| 密码哈希 | bcryptjs |
| Key 加密 | AES-256-GCM |
| 并行启动 | concurrently |

---

## 3. 项目结构

```
AI作业3/
├── 设计文档.md              # 架构/接口/验收标准设计文档
├── 使用文档.md              # 本文档
├── package.json             # 根工作区（concurrently 并行启动）
├── .env.example             # 环境变量示例
├── .gitignore
├── deepseek_api.md          # DeepSeek API Key 来源文件
│
├── backend/                 # 后端 NestJS
│   ├── package.json
│   ├── tsconfig.json
│   ├── nest-cli.json
│   ├── .env                 # JWT_SECRET / AES_KEY / CORS 等
│   ├── prisma/
│   │   ├── schema.prisma    # User / ApiKey / History 三张表
│   │   └── dev.db           # SQLite 数据库文件（自动生成）
│   └── src/
│       ├── main.ts          # 入口（全局前缀 /api、CORS、日志脱敏）
│       ├── app.module.ts    # 根模块
│       ├── app.controller.ts # /api/health
│       ├── core.module.ts   # 全局：AppConfig / PrismaService / CryptoUtil
│       ├── common/
│       │   └── config.ts    # 读取环境变量
│       ├── prisma.service.ts
│       ├── crypto/
│       │   └── crypto.util.ts # AES-256-GCM 加解密 + 掩码
│       ├── auth/             # 注册 / 登录 / JWT 守卫
│       │   ├── auth.module.ts
│       │   ├── auth.controller.ts
│       │   ├── auth.service.ts
│       │   ├── jwt.strategy.ts
│       │   ├── jwt-auth.guard.ts
│       │   └── dto/auth.dto.ts
│       ├── chat/             # 非流式 + SSE 流式转发
│       │   ├── chat.module.ts
│       │   ├── chat.controller.ts
│       │   └── chat.service.ts
│       ├── api-key/          # 用户 Key 加密存取
│       │   ├── api-key.module.ts
│       │   └── api-key.controller.ts
│       ├── history/          # 历史记录 CRUD
│       │   ├── history.module.ts
│       │   ├── history.controller.ts
│       │   └── dto/create-history.dto.ts
│       ├── providers/        # 厂商 / 模型列表
│       │   ├── providers.module.ts
│       │   └── providers.controller.ts
│       └── adapters/         # 厂商适配器层（扩展点）
│           ├── adapter.interface.ts  # 统一接口
│           ├── deepseek.adapter.ts   # DeepSeek 实现
│           └── provider.registry.ts  # 适配器注册表
│
└── frontend/                 # 前端 React + Vite
    ├── package.json
    ├── tsconfig.json
    ├── tsconfig.node.json
    ├── vite.config.ts        # 端口 5173 + 代理 /api 到 8787
    ├── index.html
    └── src/
        ├── main.tsx          # React 入口
        ├── App.tsx           # 路由 + 鉴权守卫
        ├── AppLayout.tsx     # 顶部栏 + 菜单
        ├── theme.ts          # MUI 主题
        ├── api/
        │   ├── client.ts     # axios 实例 + JWT 拦截器 + 401 跳登录
        │   ├── types.ts      # 共享类型定义
        │   ├── auth.ts       # 注册/登录/me
        │   ├── provider.ts   # 厂商/Key
        │   └── chat.ts       # 非流式 + SSE 流式 + 历史记录
        ├── stores/
        │   ├── auth.ts       # 登录态 store
        │   └── playground.ts # 主操作 store（参数/消息/结果）
        ├── pages/
        │   ├── Login.tsx
        │   ├── Register.tsx
        │   ├── Playground.tsx # 主操作页
        │   └── History.tsx
        └── components/
            ├── ProviderSelect.tsx # 厂商/模型/BaseURL/Key
            ├── ParamPanel.tsx     # temperature/top_p/max_tokens/stream
            ├── MessageEditor.tsx # system + 多轮消息增删改
            └── ResultViewer.tsx   # 可视化/原始JSON双视图 + 耗时/token
```

---

## 4. 环境要求

| 项 | 要求 |
|---|---|
| Node.js | v18.18+（推荐 v22） |
| npm | v9+（推荐 v10） |
| 浏览器 | Chrome / Edge / Firefox 最新版 |
| 操作系统 | Windows / macOS / Linux 均可 |
| 端口 | 前端 5173、后端 8787 需未被占用 |

---

## 5. 启动方式

### 5.1 首次启动（完整流程）

```bash
# 1. 进入项目根目录
cd AI作业3

# 2. 安装根依赖（concurrently）
npm install

# 3. 安装前后端依赖
npm run install:all
# 等价于：
#   npm --prefix frontend install
#   npm --prefix backend install

# 4. 初始化数据库（生成 prisma/dev.db + 表结构）
npm run db:generate      # 生成 Prisma Client
npm run db:migrate       # 建表
# 或在 backend/ 下执行：
#   npx prisma db push
```

### 5.2 日常启动（一条命令并行前后端）

在项目根目录执行：

```bash
npm run dev
```

该命令用 `concurrently` 同时启动：
- **前端**：http://localhost:5173 （Vite）
- **后端**：http://localhost:8787/api （NestJS）

浏览器访问 http://localhost:5173 即可使用。

### 5.3 单独启动（调试用）

```bash
# 只起后端
npm run dev:be
# 只起前端
npm run dev:fe
```

### 5.4 环境变量

后端配置文件位于 `backend/.env`（已带默认值，生产部署请修改）：

| 变量 | 说明 | 默认 |
|---|---|---|
| JWT_SECRET | JWT 签名密钥 | dev-jwt-secret-... |
| AES_KEY | API Key 加密密钥（32 字节 hex） | 0123... |
| FRONTEND_ORIGIN | CORS 允许的前端地址 | http://localhost:5173 |
| ALLOW_CUSTOM_BASEURL | 是否允许自定义 BaseURL | false |
| PORT | 后端端口 | 8787 |

### 5.5 停止

`Ctrl+C` 终止 `npm run dev`，前后端同时停止。

### 5.6 重置数据库

```bash
cd backend
rm prisma/dev.db            # 删库
npx prisma db push          # 重建
```

---

## 6. 测试账号

已预置一个测试账号，DeepSeek API Key 已加密保存到该账号：

| 项 | 值 |
|---|---|
| 邮箱 | `test@example.com` |
| 密码 | `123456` |
| 已绑定 Key | DeepSeek（`sk-****7ef2`） |

直接用此账号登录即可调用 DeepSeek，无需再粘贴 Key。

如需新建账号：访问 `/register` 自行注册。每个用户的 Key 与历史记录相互隔离。

---

## 7. 使用指南

### 7.1 登录

1. 打开 http://localhost:5173 ，自动跳转到 `/login`
2. 输入测试账号 `test@example.com` / `123456`，点「登录」
3. 登录成功跳转到 Playground 主页

### 7.2 配置 API Key（可选）

测试账号已存好 Key，可跳过本步。新账号需配置：

1. 在左侧「API Key」输入框粘贴你的 DeepSeek Key（`sk-` 开头）
2. 点击输入框右侧的 **保存图标（💾）**，Key 会以 AES-256-GCM 加密存入数据库
3. 输入框下方提示「已保存」，明文不再回显，只显示掩码 `sk-****xxxx`

> 也可不保存，直接在输入框里填 Key 发送（一次性使用，刷新即丢失）。

### 7.3 选择厂商与模型

顶部下拉框：
- **厂商**：首版只有 `deepseek`
- **模型**：`deepseek-chat`（通用对话）或 `deepseek-reasoner`（带推理过程）

### 7.4 调整参数

左侧「参数」面板：
- **temperature**：0–2，越小越确定，越大越发散
- **top_p**：0–1，核采样
- **max_tokens**：最大生成 token 数
- **stream**：开关流式输出

### 7.5 编辑消息

左侧「消息列表」：
- 顶部「System Prompt」：可选，填角色设定（如「你是一个翻译助手」）
- 消息列表：每条可选 role（system/user/assistant）+ 内容，支持增删改
- 默认带一条 `user` 消息示例

### 7.6 发送请求

点右上角「发送」按钮：
- **非流式模式**：等待完整返回后一次性展示，底部显示总耗时与 token 数
- **流式模式（stream 开关打开）**：右侧逐字打印，结束后显示首 token 耗时、总耗时、token 数

### 7.7 查看结果

右侧两个 Tab：
- **可视化**：AI 回答的纯文本展示
- **原始 JSON**：DeepSeek 返回的完整原始响应（调试必看）

底部芯片条：首 token 耗时 / 总耗时 / prompt tokens / completion tokens / total tokens / 厂商·模型。

### 7.8 复制 cURL / 请求 JSON

- 点「cURL」按钮：复制等价 curl 命令到剪贴板，可直接在终端粘贴执行
- 点「请求 JSON」按钮：复制本次请求体 JSON，方便贴到代码里

### 7.9 历史记录

- 每次发送成功后自动保存到历史（按用户隔离）
- 顶部栏点「历史」进入历史页
- 每条历史显示标题、厂商、模型、是否流式、耗时、token 数
- 点某条的 **载入图标（↻）**：一键回填参数 + 消息 + 响应到 Playground，可重新查看或修改后重发
- 支持单条删除与一键清空

### 7.10 自定义 BaseURL

左侧勾选「自定义 BaseURL」可填第三方兼容 OpenAI 协议的服务地址（需后端 `.env` 中 `ALLOW_CUSTOM_BASEURL=true`，默认关闭防 SSRF）。

### 7.11 退出登录

顶部栏右上角点用户邮箱 → 「退出登录」，跳回登录页。

---

## 8. 验收项

全部 13 项功能验收 + 5 项非功能验收均已通过：

### 8.1 功能验收

| 编号 | 用例 | 结果 |
|---|---|---|
| AC1 | 注册新用户 → 登录 | ✅ 登录成功跳转 Playground |
| AC2 | 错误邮箱/密码登录 | ✅ 提示凭证错误，不跳转 |
| AC3 | 未登录访问 /playground | ✅ 自动跳 /login |
| AC4 | 非流式发送一条 user 消息 | ✅ 返回完整回答，原始 JSON 可见 |
| AC5 | 开启 stream 流式 | ✅ 逐字打印，结束后展示 usage 与耗时 |
| AC6 | temperature 0.2 vs 1.0 对比 | ✅ 两次结果有差异 |
| AC7 | 三轮消息（system+user+assistant） | ✅ 多轮上下文正确接收 |
| AC8 | 填入错误 Key | ✅ 右侧显示清晰错误信息与状态码 |
| AC9 | 复制 cURL | ✅ 终端粘贴可复现请求 |
| AC10 | 历史记录回填 | ✅ 一键回填参数+消息+响应 |
| AC11 | 用户 A 数据对用户 B 不可见 | ✅ 按 userId 隔离 |
| AC12 | 退出重登 | ✅ Key 与历史仍存在（存数据库） |
| AC13 | 自定义 BaseURL | ✅ 兼容 OpenAI 协议的第三方服务可正常返回 |

### 8.2 非功能验收

| 编号 | 项 | 结果 |
|---|---|---|
| NF-AC1 | 一条命令启动 | ✅ `npm run dev` |
| NF-AC2 | 浏览器兼容 | ✅ Chrome/Edge 最新版正常 |
| NF-AC3 | 安全 | ✅ 日志与数据库无 Key 明文 |
| NF-AC4 | 可扩展 | ✅ 新增厂商仅 1 文件 + 1 行注册 |
| NF-AC5 | 性能 | ✅ 流式首 token ≤ 5s |

### 8.3 实测数据

| 指标 | 实测值 |
|---|---|
| 非流式调用耗时 | ~484 ms（含网络） |
| 流式首 token 耗时 | ~1076 ms |
| 流式总耗时 | ~1317 ms（28 tokens） |
| token 统计 | prompt 8 / completion 28 / total 36（准确） |
| 前端控制台错误 | 无 JS 错误 |

---

## 9. 后端 API 速查

所有接口前缀 `/api`，除 `/health` 外需 `Authorization: Bearer <token>`。

### 9.1 认证

| 方法 | 路径 | 说明 | 请求体 |
|---|---|---|---|
| POST | `/auth/register` | 注册 | `{ email, password }` |
| POST | `/auth/login` | 登录 | `{ email, password }` → `{ accessToken, user }` |
| GET | `/auth/me` | 当前用户 | - |

### 9.2 厂商与 Key

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/providers` | 厂商列表（含默认 baseURL + 模型列表） |
| GET | `/providers/:id/models` | 指定厂商的模型列表 |
| GET | `/api-keys/:provider` | 读取已保存 Key（返回掩码） |
| PUT | `/api-keys/:provider` | 保存/更新 Key（加密入库） |
| DELETE | `/api-keys/:provider` | 删除 Key |

### 9.3 调用

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/chat` | 非流式调用，返回完整 ChatResponse |
| POST | `/chat/stream` | 流式调用，返回 SSE 事件流 |

**ChatRequest 请求体**：
```json
{
  "provider": "deepseek",
  "baseUrl": "https://api.deepseek.com",
  "apiKey": "sk-xxx",
  "model": "deepseek-chat",
  "messages": [
    { "role": "system", "content": "你是一个翻译助手" },
    { "role": "user", "content": "你好" }
  ],
  "params": { "temperature": 0.7, "top_p": 1, "max_tokens": 1024, "stream": false }
}
```

**SSE 事件流**（`/chat/stream`）：
```
data: {"type":"delta","content":"你"}
data: {"type":"delta","content":"好"}
data: {"type":"done","usage":{...},"timing":{...},"raw":{...}}
data: {"type":"error","message":"...","status":401}
```

### 9.4 历史记录

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/history` | 新建一条（前端发送成功后自动调用） |
| GET | `/history` | 当前用户历史列表（最近 100 条） |
| GET | `/history/:id` | 单条详情 |
| DELETE | `/history/:id` | 删除单条 |
| DELETE | `/history` | 清空本人历史 |

### 9.5 健康

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/health` | 健康检查，无需鉴权 |

---

## 10. 安全说明

1. **密码**：bcryptjs 哈希存储，不存明文
2. **API Key 加密**：AES-256-GCM 加密入库；解密仅在调用时内存中进行
3. **Key 永不回显明文**：GET 接口只返回掩码 `sk-****xxxx`
4. **日志脱敏**：后端日志中 `apiKey` 字段自动替换为 `***`
5. **JWT 鉴权**：accessToken 7 天有效；前端 401 自动跳登录
6. **多用户隔离**：所有 Key / 历史查询按 `req.user.id` 过滤
7. **SSRF 防护**：自定义 BaseURL 默认关闭（`ALLOW_CUSTOM_BASEURL=false`），开启时校验 https + 非内网
8. **CORS**：仅允许 `FRONTEND_ORIGIN` 配置的前端地址
9. **请求体限制**：后端限制 ≤ 1MB

---

## 11. 扩展厂商

新增一个厂商只需 **2 步**：

**第 1 步**：在 [backend/src/adapters/](backend/src/adapters) 新建一个适配器文件，实现 `LlmAdapter` 接口：

```ts
// adapters/openai.adapter.ts
import { Injectable } from '@nestjs/common';
import { LlmAdapter, ChatRequest, ChatUsage, StreamDelta } from './adapter.interface';

@Injectable()
export class OpenAiAdapter implements LlmAdapter {
  readonly name = 'openai';
  readonly defaultBaseUrl = 'https://api.openai.com/v1';
  readonly models = ['gpt-4o', 'gpt-4o-mini'];

  buildRequest(req: ChatRequest, apiKey: string) {
    // 构造 HTTP 请求
  }
  parseResponse(raw: any) {
    // 解析非流式响应
  }
  parseStreamChunk(chunkStr: string): StreamDelta[] {
    // 解析 SSE chunk
  }
}
```

**第 2 步**：在 [provider.registry.ts](backend/src/adapters/provider.registry.ts) 注册一行：

```ts
constructor(
  private readonly deepseek: DeepSeekAdapter,
  private readonly openai: OpenAiAdapter,   // 新增
) {
  this.register(deepseek);
  this.register(openai);                     // 新增
}
```

完成后前端下拉框自动出现该厂商，无需改前端代码。

---

## 12. 常见问题

### Q1：启动报端口被占用？

修改 `backend/.env` 的 `PORT` 和 `frontend/vite.config.ts` 的 `server.port`，两边都要改，并把 `vite.config.ts` 的 proxy target 同步改到新后端端口。

### Q2：bcrypt / bcryptjs 区别？

原设计用 `bcrypt`（原生 C++ 模块），但 Windows 下无 VS C++ 工具链会编译失败。项目已改用 `bcryptjs`（纯 JS 实现），功能等价、免编译、跨平台稳定。

### Q3：忘记密码怎么办？

数据库里的密码是哈希不可逆的。直接重置数据库即可：
```bash
cd backend && rm prisma/dev.db && npx prisma db push
```
然后重新注册账号。

### Q4：DeepSeek 返回 model 是 `deepseek-flash` 而不是 `deepseek-chat`？

这是 DeepSeek 服务端的别名映射（`deepseek-chat` 调用后实际由 `deepseek-flash` 接待），不影响使用，请求与计费按你填的 `deepseek-chat` 走。

### Q5：流式响应看起来卡顿？

首 token 通常 1 秒左右，后续逐字推送。若整体很慢，通常是 DeepSeek 服务端或网络问题，可在「原始 JSON」Tab 查看完整响应确认。

### Q6：如何彻底清除所有数据？

```bash
cd backend
rm prisma/dev.db
npx prisma db push
```
这会删掉所有用户、Key、历史。前端 localStorage 里的 token 也可在浏览器 DevTools 清掉。

### Q7：能部署到服务器吗？

可以。生产部署建议：
1. 前端 `npm run build` 产物用 nginx 托管
2. 后端 `npm run build` 后 `node dist/main.js` 跑起，用 pm2 守护
3. 强制 HTTPS，修改 `JWT_SECRET` 和 `AES_KEY` 为强随机值
4. 数据库从 SQLite 换 PostgreSQL（改 `schema.prisma` 的 `datasource.provider`）
5. `FRONTEND_ORIGIN` 改成实际前端域名

---

## 附：相关文件链接

- [设计文档.md](设计文档.md)
- [backend 源码](backend/src)
- [frontend 源码](frontend/src)
- [Prisma Schema](backend/prisma/schema.prisma)
- [DeepSeek 适配器](backend/src/adapters/deepseek.adapter.ts)
- [Playground 主页](frontend/src/pages/Playground.tsx)
