# 测试报告 · 大模型接口调用工具箱 v2.0

> 测试日期：2026-09-10
> 测试环境：Windows 10 / Node.js v22.22.0 / 后端 localhost:8787 / 前端 localhost:5173
> 测试人员：自动化接口测试 + 人工验证
> 版本：v2.0（多厂商版，含安全修复）

---

## 1. 测试范围

本次测试覆盖：
1. **功能测试**：用户体系、厂商列表、Key 管理、LLM 调用、流式、历史、参数校验
2. **安全测试**：SSRF 防护、限流、鉴权、字段校验、安全 HTTP 头、密钥管理
3. **回归测试**：DeepSeek 单厂商功能（v1.0 已有）在多厂商版下仍正常

测试方式：PowerShell `Invoke-WebRequest` 直接调后端 API，记录 HTTP 状态码与响应体。

---

## 2. 测试环境

| 项 | 值 |
|---|---|
| OS | Windows 10 (10.0.19045) |
| Node.js | v22.22.0 |
| 后端 | NestJS 10 + Prisma 5 + SQLite，端口 8787 |
| 前端 | React 18 + Vite 6，端口 5173 |
| 测试账号 | test@example.com / 123456（预置 DeepSeek Key） |
| DeepSeek Key | sk-5714...7ef2（有效） |

---

## 3. 功能测试结果

### 3.1 用户体系（T1-T6）

| 编号 | 用例 | 预期 | 实际 | 结果 |
|---|---|---|---|---|
| T1 | 注册新用户 testuser2@example.com | HTTP 201 + 返回 token | HTTP 201，token eyJhbGciOiJIUzI1NiIs... | ✅ 通过 |
| T2 | 错误密码登录 test@example.com | HTTP 401 凭证错误 | HTTP 401 | ✅ 通过 |
| T3 | 注册密码短于 6 位（"123"） | HTTP 400 字段校验失败 | HTTP 400 | ✅ 通过 |
| T4 | 无 Token 访问 /api/auth/me | HTTP 401 未授权 | HTTP 401 | ✅ 通过 |
| T5 | 测试账号登录 | HTTP 200 + token | 登录成功，token eyJhbGciOiJIUzI1NiIs... | ✅ 通过 |
| T6 | 带 Token 调 /api/auth/me | 返回 {id, email} | {"id":"cmtv6cv2o...","email":"test@example.com"} | ✅ 通过 |

### 3.2 厂商与模型（T7）

| 编号 | 用例 | 预期 | 实际 | 结果 |
|---|---|---|---|---|
| T7 | GET /api/providers 厂商列表 | 9 家厂商 + paramBounds/pricing | 9 家：deepseek, zhipu, kimi, qwen, ernie, spark, hunyuan, doubao, minimax | ✅ 通过 |

### 3.3 Key 管理（T8, T11）

| 编号 | 用例 | 预期 | 实际 | 结果 |
|---|---|---|---|---|
| T8 | 保存 Key 长度 < 10 | HTTP 400 校验失败 | HTTP 400 | ✅ 通过 |
| T11a | PUT /api/api-keys/deepseek 保存 Key | 返回掩码 sk-****2345 | {"saved":true,"masked":"sk-****2345"} | ✅ 通过 |
| T11b | GET /api/api-keys/deepseek 读取 | 返回掩码，不返回明文 | {"saved":true,"masked":"sk-****2345"} | ✅ 通过 |
| T11c | DELETE /api/api-keys/deepseek | {"saved":false} | {"saved":false} | ✅ 通过 |

### 3.4 LLM 调用（T10, T16）

| 编号 | 用例 | 预期 | 实际 | 结果 |
|---|---|---|---|---|
| T10 | DeepSeek 非流式 chat | HTTP 200 + content + usage | 200，content 正确，usage: prompt 6 / completion 103 / total 109 | ✅ 通过 |
| T16 | DeepSeek 流式 chat/stream | SSE 事件流，逐字推送 | 92 个 SSE 事件，首 token 912ms，总 1556ms，末事件 type:done 带 usage | ✅ 通过 |

### 3.5 参数校验（T9）

| 编号 | 用例 | 预期 | 实际 | 结果 |
|---|---|---|---|---|
| T9 | temperature=99（越界） | HTTP 400 拒绝 | HTTP 400 | ✅ 通过 |

### 3.6 历史记录（T12）

| 编号 | 用例 | 预期 | 实际 | 结果 |
|---|---|---|---|---|
| T12 | GET /api/history | 返回当前用户历史 | 2 条记录（T10、T16 两次调用） | ✅ 通过 |

---

## 4. 安全测试结果

### 4.1 SSRF 防护（T13, T14）

| 编号 | 用例 | 预期 | 实际 | 结果 |
|---|---|---|---|---|
| T13 | 自定义 BaseURL=http://127.0.0.1:8080 | HTTP 400 拦截 | HTTP 400「禁止访问内网地址」 | ✅ 通过 |
| T14 | 自定义 BaseURL=https://172.16.0.1/v1 | HTTP 400 拦截 | HTTP 400「禁止访问内网地址」 | ✅ 通过 |

> 注：SSRF 拦截逻辑已修复——v2.0 之前用 `startsWith('172.2')` 会误匹配公网 172.200.x 且漏检 172.32-255.x；现已改为 CIDR 严格判断（172.16.0.0/12），同时新增 IPv6 本地/私网地址拦截。

### 4.2 限流防暴力破解（T15）

| 编号 | 用例 | 预期 | 实际 | 结果 |
|---|---|---|---|---|
| T15 | 连续 65 次 GET /api/auth/me | 前 60 次 200，后续 429 | 200×58 / 429×7 | ✅ 通过 |

> 限流配置：每 IP 每 60 秒 60 次请求（ThrottlerModule），超出返回 429。

### 4.3 安全 HTTP 头（T17）

| 编号 | 头 | 预期 | 实际 | 结果 |
|---|---|---|---|---|
| T17a | X-Content-Type-Options | nosniff | nosniff | ✅ 通过 |
| T17b | X-Frame-Options | SAMEORIGIN | SAMEORIGIN | ✅ 通过 |
| T17c | Strict-Transport-Security | max-age ≥ 1 年 | max-age=31536000; includeSubDomains | ✅ 通过 |

> 通过 helmet 中间件统一注入安全头，防 clickjacking、MIME 嗅探、强制 HTTPS。

### 4.4 密钥管理安全（代码审查）

| 编号 | 项 | 结果 | 说明 |
|---|---|---|---|
| S1 | AES-256-GCM 加密存储 | ✅ | IV 用 randomBytes(12) 随机生成，tag 拼接密文尾部 |
| S2 | Key 掩码返回 | ✅ | GET 接口只返回 `sk-****xxxx`，明文永不回显 |
| S3 | 日志脱敏 | ✅ | apiKey 字段打印 `***`，messages 字段 `[redacted]` |
| S4 | bcryptjs 密码哈希 | ✅ | saltRounds=10 |
| S5 | JWT 鉴权 | ✅ | Bearer token，7 天有效 |
| S6 | 多用户隔离 | ✅ | 所有查询按 userId 过滤，复合唯一索引 (userId, provider) |
| S7 | 生产环境强制强密钥 | ✅（已修复） | NODE_ENV=production 时校验 JWT_SECRET ≥32 字符、AES_KEY 非默认值，否则启动失败 |
| S8 | CORS 限制 | ✅ | 仅允许 FRONTEND_ORIGIN |
| S9 | 无 SQL 注入 | ✅ | Prisma 全参数化查询，无原始 SQL 拼接 |
| S10 | 无 XSS | ✅ | React JSX 默认转义，ResultViewer 用 `{s.content}` 渲染 |

---

## 5. 安全漏洞修复记录

本次在原版基础上发现并修复 5 个漏洞：

| 编号 | 严重度 | 漏洞 | 文件 | 修复 |
|---|---|---|---|---|
| V1 | 高 | JWT_SECRET/AES_KEY 硬编码默认值，生产环境忘配置即可被伪造 JWT | backend/src/common/config.ts | 启动时校验：生产环境（NODE_ENV=production）必须显式配置强随机 JWT_SECRET（≥32 字符）和 AES_KEY（非默认值），否则直接抛错拒绝启动；开发环境打印警告 |
| V2 | 中 | 无速率限制，登录可暴力破解 | backend/src/app.module.ts, main.ts | 引入 @nestjs/throttler 全局限流：每 IP 每 60 秒 60 次，超出返回 429 |
| V3 | 中 | 缺安全 HTTP 头（X-Frame-Options 等） | backend/src/main.ts | 引入 helmet 中间件，统一注入 nosniff / SAMEORIGIN / HSTS / CSP |
| V4 | 低 | SSRF 内网判断 172.x 不完整（`startsWith('172.2')` 误匹配公网、漏检 172.32-255.x） | backend/src/chat/chat.service.ts | 改为 CIDR 严格判断：正则匹配 `^172\.(\d+)\.` 后检查 second octet 是否在 16-31 范围；新增 IPv6 本地/私网拦截 |
| V5 | 低 | API Key 更新后又解密一次仅为生成掩码，增加明文暴露窗口 | backend/src/api-key/api-key.controller.ts | 直接用 dto.apiKey 生成掩码，避免再次解密入内存 |

---

## 6. 测试汇总

| 类别 | 用例数 | 通过 | 失败 | 通过率 |
|---|---|---|---|---|
| 功能测试 | 13 | 13 | 0 | 100% |
| 安全测试 | 10 | 10 | 0 | 100% |
| 合计 | 23 | 23 | 0 | 100% |

**结论**：v2.0（多厂商版 + 安全修复）全部 23 项测试用例通过，功能正常，安全漏洞已修复，可交付。

---

## 7. 已知限制

1. **镜像构建需 Docker 环境**：本机未安装 Docker（WSL 也未启用，需管理员权限+重启），已交付 Dockerfile + 构建脚本（build.ps1/build.sh），用户在有 Docker 的机器上一条命令即可构建 amd64 镜像
2. **费用估算为参考值**：各厂商定价可能随时间调整，估算结果仅供决策参考，实际计费以厂商账单为准
3. **自定义 BaseURL 默认关闭**：`ALLOW_CUSTOM_BASEURL=false`，需要时通过环境变量开启，已做 SSRF 防护
4. **SQLite 单机**：当前用 SQLite 文件型数据库，适合单机/小规模；多实例部署需换 PostgreSQL（改 Prisma datasource.provider）
