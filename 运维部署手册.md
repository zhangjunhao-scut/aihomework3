# 运维部署手册 · 大模型接口调用工具箱 v2.0

> 版本：v2.0（多厂商版）
> 日期：2026-09-10
> 适用对象：负责部署、运维、监控的工程师
> 目标：让运维同事能独立完成项目部署、镜像构建、容器编排、日常运维

---

## 目录

- [1. 部署架构总览](#1-部署架构总览)
- [2. 环境要求](#2-环境要求)
- [3. 镜像构建（amd64）](#3-镜像构建amd64)
- [4. 容器编排启动](#4-容器编排启动)
- [5. 环境变量配置](#5-环境变量配置)
- [6. 数据持久化与备份](#6-数据持久化与备份)
- [7. 健康检查与监控](#7-健康检查与监控)
- [8. 日志管理](#8-日志管理)
- [9. 升级与回滚](#9-升级与回滚)
- [10. 安全加固清单](#10-安全加固清单)
- [11. 故障排查](#11-故障排查)
- [12. 离线部署（无外网环境）](#12-离线部署无外网环境)

---

## 1. 部署架构总览

### 1.1 容器化架构

```
                  ┌──────────────────────────┐
                  │  用户浏览器                │
                  │  http://<服务器IP>         │
                  └────────────┬──────────────┘
                               │
                               ▼
                  ┌──────────────────────────┐
                  │  frontend 容器（nginx）   │
                  │  端口：80                │
                  │  - 静态托管 React 产物   │
                  │  - /api/ 反代到 backend  │
                  │  - SSE 流式不缓冲         │
                  └────────────┬──────────────┘
                               │ 内部网络
                               ▼
                  ┌──────────────────────────┐
                  │  backend 容器（NestJS）   │
                  │  端口：8787（仅 expose） │
                  │  - JWT 鉴权               │
                  │  - 9 家厂商适配器         │
                  │  - Throttler 限流         │
                  │  - helmet 安全头          │
                  └────────────┬──────────────┘
                               │
              ┌────────────────┼────────────────┐
              ▼                                  ▼
   ┌────────────────────┐         ┌────────────────────────┐
   │  Volume 持久化      │         │  各家 LLM API 服务      │
   │  /app/data/dev.db   │         │  (DeepSeek/智谱/Kimi/...)│
   │  SQLite 数据库       │         │  出站 HTTPS             │
   └────────────────────┘         └────────────────────────┘
```

### 1.2 容器清单

| 容器 | 镜像 | 端口 | 说明 |
|---|---|---|---|
| aihomework3-frontend | aihomework3-frontend:amd64 | 80:80 | nginx 静态托管 + 反代 |
| aihomework3-backend | aihomework3-backend:amd64 | 8787（内部） | NestJS API |

### 1.3 数据流

1. 浏览器访问 `http://<服务器IP>/`，nginx 返回 React SPA
2. SPA 发 `/api/...` 请求，nginx 反代到 backend 容器
3. backend 鉴权后调用对应厂商 LLM API，返回结果
4. 流式调用：nginx 关闭 buffering，SSE 事件逐字透传到浏览器
5. 用户数据、Key 加密、历史记录存 backend 容器挂载卷的 SQLite

---

## 2. 环境要求

### 2.1 主机要求

| 项 | 最低 | 推荐 |
|---|---|---|
| CPU | 1 核 | 2 核 |
| 内存 | 512 MB | 1 GB |
| 磁盘 | 1 GB | 5 GB（含日志与数据库） |
| 架构 | **linux/amd64** | linux/amd64 |
| OS | Linux 任意发行版 / Windows + Docker Desktop | Ubuntu 22.04+ |

### 2.2 软件要求

| 软件 | 版本 | 用途 |
|---|---|---|
| Docker Engine | 24.0+ | 容器运行时 |
| Docker Compose | v2.20+ | 容器编排 |
| （可选）nginx | 1.20+ | 前置反代/TLS 终止（生产推荐） |

### 2.3 网络要求

- **入站**：80 端口（前端），或 443（加 TLS 时）
- **出站**：HTTPS 443 到以下域名（调用各家 LLM API）：
  - `api.deepseek.com`
  - `open.bigmodel.cn`
  - `api.moonshot.cn`
  - `dashscope.aliyuncs.com`
  - `qianfan.baidubce.com`
  - `spark-api-open.xf-yun.com`
  - `api.hunyuan.cloud.tencent.com`
  - `ark.cn-beijing.volces.com`
  - `api.minimaxi.com`

---

## 3. 镜像构建（amd64）

### 3.1 构建方式

项目根目录已提供：
- `backend/Dockerfile` —— 后端多阶段构建（builder + runner）
- `frontend/Dockerfile` —— 前端多阶段构建（node builder + nginx runner）
- `frontend/nginx.conf` —— nginx 静态托管 + /api 反代 + SSE 优化
- `build.ps1` —— Windows 一键构建脚本
- `build.sh` —— Linux/macOS 一键构建脚本
- `docker-compose.yml` —— 容器编排

### 3.2 一键构建（推荐）

**Windows PowerShell**：
```powershell
cd AI作业3
.\build.ps1
```

**Linux/macOS**：
```bash
cd AI作业3
bash build.sh
```

脚本会：
1. 校验 docker 可用
2. `docker build --platform linux/amd64` 构建后端镜像 `aihomework3-backend:amd64`
3. `docker build --platform linux/amd64` 构建前端镜像 `aihomework3-frontend:amd64`
4. 打印镜像列表与启动方式

### 3.3 手动构建（分步）

```bash
# 后端镜像
docker build --platform linux/amd64 -t aihomework3-backend:amd64 ./backend

# 前端镜像
docker build --platform linux/amd64 -t aihomework3-frontend:amd64 ./frontend
```

### 3.4 镜像说明

**后端镜像 aihomework3-backend:amd64**：
- 基础镜像：`node:20-bookworm-slim`
- 多阶段构建：builder 阶段编译 TS + 精简生产依赖；runner 阶段只拷贝 dist + node_modules + prisma
- 非 root 用户运行（app 用户）
- 启动命令：`npx prisma db push --skip-generate && node dist/main.js`（首次启动自动建表）
- 数据库路径：`/app/data/dev.db`（配合 volume 持久化）

**前端镜像 aihomework3-frontend:amd64**：
- 基础镜像：`nginx:alpine`
- 多阶段构建：builder 阶段 `npm run build` 生成静态文件；runner 阶段 nginx 托管
- nginx 配置：SPA 路由回退 + `/api/` 反代到 backend 容器 + SSE 流式不缓冲

### 3.5 验证镜像

构建完成后验证：
```bash
docker images | grep aihomework3
# 应看到：
# aihomework3-backend   amd64   ...
# aihomework3-frontend  amd64   ...

# 检查镜像架构
docker inspect aihomework3-backend:amd64 | grep Architecture
# 应看到 "Architecture": "amd64"
```

---

## 4. 容器编排启动

### 4.1 准备环境变量

在项目根目录创建 `.env` 文件（**不要提交到 git**）：

```bash
# 必改：生产环境必须用强随机值
# 生成方式：openssl rand -hex 32
JWT_SECRET=请替换为32字符以上的强随机字符串

# 必改：API Key 加密密钥（64 hex 字符）
# 生成方式：openssl rand -hex 32
AES_KEY=请替换为64位hex字符的强随机密钥

# 可选：是否允许自定义 BaseURL（默认 false，防 SSRF）
ALLOW_CUSTOM_BASEURL=false
```

> **重要**：生产环境必须改 JWT_SECRET 和 AES_KEY 为强随机值。后端启动时若 `NODE_ENV=production` 且这两个值是默认值或过短，会直接抛错拒绝启动（安全防护）。

### 4.2 启动

```bash
docker compose up -d
```

输出示例：
```
[+] Running 3/3
 ✔ Network aihomework3_default      Created
 ✔ Volume "aihomework3_backend-data" Created
 ✔ Container aihomework3-backend     Started
 ✔ Container aihomework3-frontend    Started
```

### 4.3 验证启动

```bash
# 查看容器状态
docker compose ps
# 两个容器都应是 Up 状态

# 后端健康检查
curl http://localhost/api/health
# 应返回：{"status":"ok","time":"..."}

# 浏览器访问
# http://localhost
# 用 test@example.com / 123456 登录
```

### 4.4 停止与删除

```bash
# 停止（保留数据）
docker compose stop

# 重新启动
docker compose start

# 停止并删除容器（保留 volume 数据）
docker compose down

# 停止并删除容器 + 删除数据（⚠️ 会清空所有用户/Key/历史）
docker compose down -v
```

---

## 5. 环境变量配置

### 5.1 后端环境变量

通过 `docker-compose.yml` 的 `environment` 注入，或在 `.env` 文件配置：

| 变量 | 必填 | 默认 | 说明 |
|---|---|---|---|
| NODE_ENV | 是 | production | 设为 production 触发密钥强校验 |
| PORT | 否 | 8787 | 后端监听端口 |
| JWT_SECRET | **是** | 无 | JWT 签名密钥，生产必须 ≥32 字符 |
| AES_KEY | **是** | 无 | API Key 加密密钥，64 hex 字符 |
| FRONTEND_ORIGIN | 否 | http://localhost | CORS 允许的前端地址 |
| ALLOW_CUSTOM_BASEURL | 否 | false | 是否允许用户自定义 BaseURL |

### 5.2 生成强随机密钥

```bash
# JWT_SECRET（32 字符以上）
openssl rand -base64 32

# AES_KEY（64 hex 字符）
openssl rand -hex 32
```

### 5.3 修改环境变量后重启

```bash
# 修改 .env 后重新创建容器
docker compose up -d --force-recreate
```

---

## 6. 数据持久化与备份

### 6.1 数据存储位置

- SQLite 数据库文件：`backend-data` volume，容器内路径 `/app/data/dev.db`
- 包含：用户表（User）、API Key 表（ApiKey，加密）、历史记录表（History）

### 6.2 查看数据卷

```bash
docker volume ls | grep aihomework3
# aihomework3_backend-data

# 查看卷详情
docker volume inspect aihomework3_backend-data
```

### 6.3 备份数据库

```bash
# 方式一：拷贝数据库文件
docker cp aihomework3-backend:/app/data/dev.db ./backup-$(date +%Y%m%d).db

# 方式二：用 sqlite3 命令在线备份（不锁库）
docker exec aihomework3-backend npx prisma db execute --stdin < schema.sql
# 或用 sqlite3 .backup 命令（需镜像含 sqlite3）
```

### 6.4 恢复数据库

```bash
# 停止后端
docker compose stop backend

# 拷贝备份文件回去
docker cp ./backup-20260910.db aihomework3-backend:/app/data/dev.db

# 重启后端
docker compose start backend
```

### 6.5 定时备份（推荐 crontab）

```bash
# 每天凌晨 3 点备份
0 3 * * * docker cp aihomework3-backend:/app/data/dev.db /backup/aihomework3-$(date +\%Y\%m\%d).db
```

---

## 7. 健康检查与监控

### 7.1 健康检查端点

```bash
# 后端健康检查
curl http://localhost/api/health
# 返回 {"status":"ok","time":"2026-09-10T..."}
```

### 7.2 容器状态检查

```bash
# 查看容器状态
docker compose ps

# 查看资源占用
docker stats aihomework3-backend aihomework3-frontend
```

### 7.3 推荐监控项

| 指标 | 检查方式 | 告警阈值 |
|---|---|---|
| 容器存活 | `docker compose ps` | 非 Up 状态 |
| 后端健康 | `curl /api/health` | 非 200 |
| CPU | `docker stats` | > 80% 持续 5 分钟 |
| 内存 | `docker stats` | > 80% |
| 磁盘 | `df -h` | > 80% |
| 429 限流 | 日志统计 | 突增 |
| LLM 调用失败 | 日志统计 | > 10% |

### 7.4 接入 Prometheus（可选）

后端未内置 Prometheus exporter，可通过 nginx 日志 + 日志分析方案监控 QPS / 错误率。

---

## 8. 日志管理

### 8.1 查看实时日志

```bash
# 全部容器日志
docker compose logs -f

# 仅后端日志
docker compose logs -f backend

# 仅前端日志
docker compose logs -f frontend
```

### 8.2 日志脱敏说明

后端日志已做脱敏处理：
- 请求体 `apiKey` 字段打印为 `***`
- 请求体 `messages` 字段打印为 `[redacted]`（避免 prompt 内容泄露）
- 不打印 Authorization 头

### 8.3 日志轮转

Docker 默认 json-file 日志驱动，建议配置轮转防止撑满磁盘：

`/etc/docker/daemon.json`：
```json
{
  "log-driver": "json-file",
  "log-opts": {
    "max-size": "10m",
    "max-file": "3"
  }
}
```

修改后重启 Docker：`systemctl restart docker`

### 8.4 导出日志

```bash
# 导出近 24 小时后端日志
docker logs --since 24h aihomework3-backend > backend.log

# 导出带时间戳
docker logs -t aihomework3-backend > backend.log
```

---

## 9. 升级与回滚

### 9.1 升级流程

```bash
# 1. 拉取新代码 / 构建新镜像
git pull
docker compose build

# 2. 备份数据库（必做）
docker cp aihomework3-backend:/app/data/dev.db ./backup-before-upgrade.db

# 3. 滚动重启
docker compose up -d --no-deps --build backend

# 4. 验证
curl http://localhost/api/health
```

### 9.2 回滚流程

```bash
# 1. 停止服务
docker compose down

# 2. 恢复旧镜像（如有保留）
docker tag aihomework3-backend:amd64-prev aihomework3-backend:amd64

# 3. 恢复数据库
docker volume create aihomework3_backend-data
docker run --rm -v aihomework3_backend-data:/data -v $(pwd):/backup alpine \
  cp /backup/backup-before-upgrade.db /data/dev.db

# 4. 启动
docker compose up -d
```

### 9.3 数据库迁移

Prisma schema 变更后，启动时自动 `prisma db push` 同步表结构。如遇破坏性变更，先备份。

---

## 10. 安全加固清单

### 10.1 部署前必做

- [ ] **改 JWT_SECRET** 为 32 字符以上强随机值（`openssl rand -base64 32`）
- [ ] **改 AES_KEY** 为 64 hex 字符强随机值（`openssl rand -hex 32`）
- [ ] **NODE_ENV=production**（触发密钥强校验）
- [ ] **ALLOW_CUSTOM_BASEURL=false**（默认关闭，防 SSRF）
- [ ] **FRONTEND_ORIGIN** 改成实际前端域名
- [ ] 用 nginx 加 HTTPS（TLS 终止在前面）
- [ ] 防火墙只开 80/443，8787 不对外暴露

### 10.2 网络安全

- [ ] 后端容器只 `expose` 不 `ports` 暴露，仅前端容器通过内部网络访问
- [ ] 出站仅允许 443 到 9 家 LLM API 域名（见 2.3）
- [ ] 入站仅 80/443
- [ ] 服务器 SSH 用密钥登录，禁用密码

### 10.3 应用安全（已内置）

| 防护 | 实现位置 | 说明 |
|---|---|---|
| JWT 鉴权 | jwt-auth.guard.ts | 所有受保护接口需 Bearer token |
| 限流防暴破 | ThrottlerModule | 每 IP 每 60 秒 60 次，超出 429 |
| AES-256-GCM Key 加密 | crypto.util.ts | IV 随机，tag 校验完整性 |
| 密码 bcryptjs 哈希 | auth.service.ts | saltRounds=10 |
| SSRF 防护 | chat.service.ts | 拦截内网地址、强制 https、可关闭 |
| 参数边界校验 | chat.service.ts | 越界返回 400 |
| 安全 HTTP 头 | helmet | nosniff/SAMEORIGIN/HSTS |
| 日志脱敏 | main.ts | apiKey→***, messages→[redacted] |
| 多用户隔离 | Prisma 查询 | 全部按 userId 过滤 |
| 非 root 运行 | Dockerfile | 容器用 app 用户 |

### 10.4 定期检查

- 每月：审计用户列表、Key 使用情况
- 每季：依赖漏洞扫描 `npm audit`
- 每年：轮换 JWT_SECRET / AES_KEY（轮换需重新加密所有 Key + 所有用户重登）

---

## 11. 故障排查

### 11.1 容器无法启动

```bash
# 看启动日志
docker compose logs backend

# 常见错误：
# "生产环境必须设置强随机 JWT_SECRET" → .env 里 JWT_SECRET 未改或过短
# "EADDRINUSE" → 端口被占，改 docker-compose.yml 端口映射
# "PrismaClientInitializationError" → 数据库路径不可写，检查 volume 挂载
```

### 11.2 前端能开但登录失败

```bash
# 1. 检查后端健康
curl http://localhost/api/health

# 2. 检查 nginx 反代
docker exec aihomework3-frontend curl -I http://backend:8787/api/health
# 应返回 200，若失败说明容器间网络不通

# 3. 检查 CORS
# 浏览器 DevTools Network 看是否有 CORS 错误
# 修改后端 FRONTEND_ORIGIN 为实际访问地址
```

### 11.3 LLM 调用失败

```bash
# 看后端日志
docker compose logs backend | grep -i error

# 常见原因：
# 401/403 → 用户 Key 无效或未保存
# 429 → 限流触发（用户请求过频 或 厂商侧限流）
# 超时 → 厂商 API 不可达，检查出站网络
# ECONNREFUSED → 厂商域名无法解析，检查 DNS
```

### 11.4 流式输出不工作

```bash
# nginx 必须关闭 buffering，检查 nginx.conf
# frontend/nginx.conf 里 location /api/ 下应有：
#   proxy_buffering off;
#   proxy_cache off;
#   chunked_transfer_encoding on;

# 如仍不工作，可能是前置反代（如云厂商 SLB）缓冲了 SSE
# 解决：前置 nginx 加 X-Accel-Buffering: no
```

### 11.5 数据库锁

```bash
# SQLite 并发写偶尔会锁库，重启后端容器即可
docker compose restart backend
```

---

## 12. 离线部署（无外网环境）

### 12.1 在有网机器上构建并导出镜像

```bash
# 构建
bash build.sh

# 导出为 tar 文件
docker save aihomework3-backend:amd64 -o backend.tar
docker save aihomework3-frontend:amd64 -o frontend.tar

# 拷贝 tar 文件 + docker-compose.yml + .env 到离线服务器
scp backend.tar frontend.tar docker-compose.yml .env user@offline-server:~/
```

### 12.2 在离线服务器上加载并启动

```bash
# 加载镜像
docker load -i backend.tar
docker load -i frontend.tar

# 验证
docker images | grep aihomework3

# 修改 .env（设置强密钥）
vi .env

# 启动（compose 文件用 image 字段，不会重新 build）
docker compose up -d
```

> 注：离线部署时 `docker-compose.yml` 用的是 `image: aihomework3-xxx:amd64`，已加载镜像后会直接使用，不会尝试构建。

---

## 附：相关文档

- [设计文档](设计文档.md) — 架构与接口设计
- [用户使用手册](用户使用手册.md) — 面向最终用户的操作指南
- [测试报告](测试报告.md) — 功能与安全测试记录
- [多厂商扩展需求文档](多厂商扩展需求文档.md) — 9 家厂商适配规格

---

## 附：快速命令速查

```bash
# 构建
.\build.ps1              # Windows
bash build.sh            # Linux

# 启停
docker compose up -d     # 启动
docker compose down      # 停止删容器（保留数据）
docker compose down -v   # 停止删容器+清数据
docker compose restart   # 重启

# 日志
docker compose logs -f backend     # 实时后端日志
docker logs --since 24h aihomework3-backend  # 导出近24h日志

# 备份
docker cp aihomework3-backend:/app/data/dev.db ./backup.db

# 健康检查
curl http://localhost/api/health

# 进容器排查
docker exec -it aihomework3-backend sh
```
