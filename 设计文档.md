# 大模型接口调用工具箱 · 设计文档

> 版本：v2.0（多厂商版定稿）
> 日期：2026-09-10
> 形式：网页版应用（前后端分离，多用户）
> 定位：大模型 API 的「专门定制版 Postman」
> 范围：9 家国内主流大模型 + 用户体系 + Key 加密 + 流式 + 历史

---

## 目录

- [1. 项目背景与定位](#1-项目背景与定位)
- [2. 目标用户与使用场景](#2-目标用户与使用场景)
- [3. 功能需求清单](#3-功能需求清单)
- [4. 系统架构](#4-系统架构)
- [5. 技术选型](#5-技术选型)
- [6. 模块划分与目录结构](#6-模块划分与目录结构)
- [7. 适配器层设计](#7-适配器层设计)
- [8. 接口设计](#8-接口设计)
- [9. 数据库模型](#9-数据库模型)
- [10. 前端关键交互设计](#10-前端关键交互设计)
- [11. 安全设计](#11-安全设计)
- [12. 验收标准](#12-验收标准)
- [13. 开发与部署](#13-开发与部署)

---

## 1. 项目背景与定位

### 1.1 一句话定义

**大模型接口调用工具箱** 是一个用于 **测试、调试、对比各家大模型 API 接口** 的网页版工具。它不是 AI 聊天软件，核心目标是让开发人员：快速构造请求参数 → 发给大模型服务商接口 → 查看原始返回结果 → 验证接口可用性与参数效果。

### 1.2 类比理解

| 工具 | 定位 |
|---|---|
| Postman / Apifox | 通用 HTTP 接口调试工具 |
| **本工具箱** | **专门针对大模型 API 定制版 Postman** |

### 1.3 核心价值

封装各家大模型接口的请求模板，**不用每次手写 curl / 写测试脚本**，点一点就能调试 API。切换厂商即切换 baseURL + 模型 + 参数边界，一套界面统一面对 9 家厂商。

### 1.4 关键决策（已确认定稿）

| 项 | 决策 |
|---|---|
| 用户体系 | 多用户（注册 / 登录 / JWT） |
| 厂商范围 | 9 家国内主流（DeepSeek、智谱 GLM、Kimi、通义千问、文心一言、星火、混元、豆包、MiniMax） |
| 接入策略 | 统一走各家 OpenAI 兼容端点，不实现原生签名协议 |
| 前端框架 | React 18 + Vite + TypeScript |
| UI 库 | Material UI（MUI）v5 |
| 后端框架 | NestJS + TypeScript |
| 数据库 | SQLite（Prisma ORM，零配置） |
| 启动方式 | 前后端一条命令并行启动 |
| 豆包 model | 用直接模型名（`doubao-1.5-pro-32k` 等），不做 ep- 自定义输入 |
| 费用估算 | 做（P1，每个适配器配 pricing） |
| Key 格式校验 | 弱提示（placeholder 提示，不强校验） |

---

## 2. 目标用户与使用场景

### 2.1 目标用户

- **后端/算法工程师**：接入大模型 API 时调试参数、验证响应格式
- **测试/前端工程师**：联调时验证 prompt 效果、查看流式返回
- **学习者**：理解大模型接口的请求/响应结构
- **多厂商对比者**：在统一界面切换 9 家厂商，对比回答质量与速度

### 2.2 典型场景

1. 拿到一个 DeepSeek / 智谱 / Kimi API Key，想快速验证它能不能通
2. 对比不同 `temperature` / `top_p` 下生成结果差异
3. 验证流式（stream）输出是否逐字返回
4. 多轮对话构造（system / user / assistant 消息数组）
5. 对比同一 prompt 在不同厂商下的回答质量
6. 复制请求体 JSON 给同事或贴到代码里
7. 多人各自保存各自的 Key 与历史记录（多用户隔离）
8. 查看每次请求的预估费用，辅助成本决策

---

## 3. 功能需求清单

### 3.1 核心功能（P0，已实现）

| 编号 | 功能 | 描述 |
|---|---|---|
| F1 | 用户注册/登录 | 邮箱+密码注册、JWT 登录、bcryptjs 密码哈希 |
| F2 | 多厂商切换 | 9 家厂商适配器，下拉切换即换 baseURL + 模型 + 参数边界 |
| F3 | 密钥管理 | 每用户每厂商独立保存 Key，AES-256-GCM 加密入库，掩码回显 |
| F4 | 模型选择 | 按厂商下拉选择模型（如 glm-4 / moonshot-v1-128k / qwen-max） |
| F5 | 动态参数面板 | temperature/top_p/max_tokens 范围随厂商自动适配；不支持 top_p 的厂商自动隐藏 |
| F6 | 消息编辑 | 增删改 system / user / assistant 消息数组（多轮对话） |
| F7 | 发送请求 | 非流式 + 流式（SSE）两种模式 |
| F8 | 结果展示 | 可视化 + 原始 JSON 双视图；流式模式逐字打印 |
| F9 | 历史记录 | 按用户隔离保存，支持一键回填重发、单条删除、一键清空 |
| F10 | 复制/导出 | 一键复制 cURL 命令、复制请求体 JSON |
| F11 | 参数边界校验 | 后端按 `paramBounds` 校验越界值，返回 400 + 清晰错误 |

### 3.2 增强功能（P1，已实现）

| 编号 | 功能 | 描述 |
|---|---|---|
| F12 | Token 计数 | 展示 prompt / completion / total tokens |
| F13 | 响应耗时统计 | 首 token 耗时、总耗时 |
| F14 | 自定义 Endpoint | 支持填 BaseURL（用于代理 / 兼容 OpenAI 协议的第三方），默认关闭防 SSRF |
| F15 | 费用估算 | 根据各厂商 pricing + token 用量计算预估费用 |
| F16 | Key 格式提示 | 切换厂商时输入框下方显示该厂商 Key 格式提示 |

### 3.3 非功能需求

| 编号 | 类别 | 要求 |
|---|---|---|
| NF1 | 性能 | 非流式 60s 内返回；流式首 token ≤ 5s |
| NF2 | 兼容性 | Chrome / Edge / Firefox 最新版可用 |
| NF3 | 安全 | Key 加密存数据库；JWT 鉴权；后端转发不记录 Key |
| NF4 | 可部署 | 一条 `npm run dev` 同时启动前后端 |
| NF5 | 可扩展 | 新增厂商仅 1 个适配器文件（~30 行）+ 2 处注册 |

---

## 4. 系统架构

### 4.1 架构总览

```
┌──────────────────────────────────────────────────────┐
│              浏览器（React SPA + MUI）                 │
│  ┌──────────────────────────────────────────────────┐ │
│  │  未登录：登录 / 注册页                              │ │
│  │  已登录：Playground（厂商/Key/模型/参数/消息/结果）  │ │
│  │           历史记录页                               │ │
│  └──────────────────────────────────────────────────┘ │
└───────────────────────┬──────────────────────────────┘
                        │  HTTP / SSE + JWT(Authorization)
                        ▼
┌──────────────────────────────────────────────────────┐
│           后端（NestJS / TypeScript）                  │
│  ┌──────────────────────────────────────────────────┐ │
│  │  Auth Module   注册/登录/JWT 守卫                  │ │
│  │  Chat Module   /chat(非流式) /chat/stream(SSE)    │ │
│  │  History Module 历史记录 CRUD（按用户隔离）        │ │
│  │  Providers Module 厂商/模型列表 + 参数边界        │ │
│  │  ApiKey Module  用户 Key 加密读写                  │ │
│  └──────────────────────────────────────────────────┘ │
│  ┌──────────────────────────────────────────────────┐ │
│  │  adapters/  厂商适配器层（统一 in/out 格式）       │ │
│  │  OpenAiCompatibleAdapter（基类）                  │ │
│  │  ├ DeepSeekAdapter                                │ │
│  │  ├ ZhipuAdapter / KimiAdapter / QwenAdapter       │ │
│  │  ├ ErnieAdapter / SparkAdapter / HunyuanAdapter   │ │
│  │  └ DoubaoAdapter / MinimaxAdapter                 │ │
│  └──────────────────────────────────────────────────┘ │
└───────────────┬───────────────────────┬──────────────┘
                │ Prisma                 │ HTTPS 转发
                ▼                        ▼
   ┌─────────────────────┐   ┌───────────────────────┐
   │  SQLite 数据库       │   │ 各家 LLM API 服务       │
   │  (prisma/dev.db)     │   │ (DeepSeek/智谱/Kimi/...)│
   └─────────────────────┘   └───────────────────────┘
```

### 4.2 为什么需要后端

1. **CORS**：大模型接口多数不允许浏览器直接跨域调用
2. **流式转发**：SSE 在后端中转更稳定，避免浏览器长连接乱序
3. **统一适配**：各家厂商字段不同，后端做适配层后前端只面对统一结构
4. **多用户**：需要账号系统、数据库存储历史与 Key
5. **Key 加密**：在后端用 AES-256-GCM 加密入库，浏览器永远拿不到明文

### 4.3 请求流转

```
前端构造 ChatRequest
  → POST /api/chat 或 /api/chat/stream（带 JWT）
  → JwtAuthGuard 验证身份
  → ChatService.resolveAdapter(provider) 取适配器
  → ChatService.validateParams(adapter, req) 边界校验
  → ChatService.validateBaseUrl(req) 防 SSRF
  → ChatService.resolveApiKey(userId, req) 解密取 Key
  → adapter.buildRequest(req, apiKey) 构造厂商 HTTP 请求
  → fetch(厂商 API) → 适配器 parseResponse / parseStreamChunk
  → 统一 ChatResponse / SSE 事件流 返回前端
```

---

## 5. 技术选型

| 层 | 技术 | 选型理由 |
|---|---|---|
| 前端框架 | React 18 + Vite 6 + TypeScript | 生态成熟、类型安全、HMR 快 |
| UI 组件 | MUI (Material UI) v5 | Material Design 风格，组件齐全 |
| 状态管理 | Zustand | 轻量，比 Redux 简洁 |
| 路由 | React Router v6 | React 标配 |
| 请求库 | axios + fetch | axios 拦截器加 JWT；fetch 读 SSE 流 |
| 后端框架 | NestJS 10 + TypeScript | 模块化、依赖注入、守卫/拦截器原生支持 |
| ORM | Prisma 5 | TS 友好、迁移工具好、SQLite 零配置 |
| 数据库 | SQLite（文件型） | 单机场景零配置；后续可换 PostgreSQL |
| 认证 | JWT (@nestjs/jwt) | 无状态、前后端分离适配，7 天有效 |
| 密码哈希 | bcryptjs | 纯 JS 实现，跨平台免编译（替代 bcrypt） |
| Key 加密 | AES-256-GCM (crypto 模块) | 业内标准对称加密，带认证 |
| 并行启动 | concurrently | 一条命令起前后端 |

---

## 6. 模块划分与目录结构

### 6.1 项目根目录

```
AI作业3/
├── 设计文档.md              # 本文档
├── 用户使用手册.md           # 面向用户的使用手册
├── 多厂商扩展需求文档.md      # 扩展规格说明
├── package.json             # 根工作区（concurrently 并行启动）
├── .env.example             # 环境变量示例
├── .gitignore
├── frontend/                # React + Vite + TS + MUI
└── backend/                 # NestJS + Prisma + SQLite
```

### 6.2 后端目录（NestJS）

```
backend/
├── prisma/
│   ├── schema.prisma         # User / ApiKey / History 模型
│   ├── migrations/          # 迁移文件
│   └── dev.db               # SQLite 数据库（.gitignore 排除）
├── .env                     # JWT_SECRET / AES_KEY（.gitignore 排除）
├── nest-cli.json
├── tsconfig.json
├── package.json
└── src/
    ├── main.ts              # 入口（全局前缀 /api、CORS、日志脱敏）
    ├── app.module.ts        # 根模块
    ├── app.controller.ts    # /api/health
    ├── core.module.ts       # 全局：AppConfig / PrismaService / CryptoUtil
    ├── common/
    │   └── config.ts        # 读取环境变量
    ├── prisma.service.ts    # Prisma 客户端封装
    ├── crypto/
    │   └── crypto.util.ts   # AES-256-GCM 加解密 + 掩码
    ├── auth/                # 注册 / 登录 / JWT
    │   ├── auth.module.ts
    │   ├── auth.controller.ts
    │   ├── auth.service.ts
    │   ├── jwt.strategy.ts
    │   ├── jwt-auth.guard.ts
    │   └── dto/auth.dto.ts
    ├── chat/                # 非流式 + SSE 流式转发
    │   ├── chat.module.ts
    │   ├── chat.controller.ts
    │   └── chat.service.ts
    ├── api-key/             # 用户 Key 加密存取
    │   ├── api-key.module.ts
    │   └── api-key.controller.ts
    ├── history/             # 历史记录 CRUD
    │   ├── history.module.ts
    │   ├── history.controller.ts
    │   └── dto/create-history.dto.ts
    ├── providers/           # 厂商 / 模型列表
    │   ├── providers.module.ts
    │   └── providers.controller.ts
    └── adapters/            # 厂商适配器层（扩展点）
        ├── adapter.interface.ts            # 统一接口
        ├── openai-compatible.adapter.ts     # OpenAI 兼容基类
        ├── deepseek.adapter.ts              # 9 家厂商适配器
        ├── zhipu.adapter.ts
        ├── kimi.adapter.ts
        ├── qwen.adapter.ts
        ├── ernie.adapter.ts
        ├── spark.adapter.ts
        ├── hunyuan.adapter.ts
        ├── doubao.adapter.ts
        ├── minimax.adapter.ts
        └── provider.registry.ts             # 适配器注册表
```

### 6.3 前端目录

```
frontend/
├── vite.config.ts           # 端口 5173 + 代理 /api 到 8787
├── index.html
├── tsconfig.json
├── tsconfig.node.json
├── package.json
└── src/
    ├── main.tsx             # React 入口
    ├── App.tsx              # 路由 + 鉴权守卫
    ├── AppLayout.tsx        # 顶部栏 + 菜单
    ├── theme.ts             # MUI 主题
    ├── api/
    │   ├── client.ts        # axios 实例 + JWT 拦截器 + 401 跳登录
    │   ├── types.ts         # 共享类型（含 ParamBounds / Pricing）
    │   ├── auth.ts          # 注册/登录/me
    │   ├── provider.ts      # 厂商列表/Key
    │   └── chat.ts          # 非流式 + SSE 流式 + 历史记录
    ├── stores/
    │   ├── auth.ts          # 登录态 store
    │   └── playground.ts    # 主操作 store（厂商/模型/参数/消息/结果/费用估算）
    ├── pages/
    │   ├── Login.tsx
    │   ├── Register.tsx
    │   ├── Playground.tsx   # 主操作页
    │   └── History.tsx
    └── components/
        ├── ProviderSelect.tsx  # 厂商/模型/BaseURL/Key（含格式提示）
        ├── ParamPanel.tsx      # 动态参数边界 + 条件隐藏 top_p
        ├── MessageEditor.tsx  # system + 多轮消息增删改
        └── ResultViewer.tsx   # 可视化/原始JSON双视图 + 耗时/token/费用
```

---

## 7. 适配器层设计

### 7.1 设计目标

把各家厂商的请求/响应格式差异封装在适配器层，让 ChatService 与前端只面对统一结构。新增厂商只需写一个极简配置类（~30 行），不复写协议代码。

### 7.2 统一接口（LlmAdapter）

```ts
// adapter.interface.ts
export interface LlmAdapter {
  readonly name: string;
  readonly defaultBaseUrl: string;
  readonly models: string[];

  // 核心方法：构造请求 / 解析响应 / 解析流式 chunk
  buildRequest(req: ChatRequest, apiKey: string): {
    url: string;
    headers: Record<string, string>;
    body: any;
  };
  parseResponse(raw: any): { content: string; usage?: ChatUsage; raw: unknown };
  parseStreamChunk(chunkStr: string): StreamDelta[];

  // 扩展字段（可选）
  readonly paramBounds?: ParamBounds;           // 参数边界，前端动态校验
  readonly supportsTopP?: boolean;              // 是否支持 top_p（ernie=false）
  readonly extraBody?: (req: ChatRequest) => Record<string, any>;  // 额外请求体（如混元 enable_enhancement）
  readonly keyHint?: string;                    // 前端 Key 输入框 placeholder 提示
  readonly keyFormat?: string;                  // Key 格式正则（弱校验）
  readonly pricing?: Pricing;                   // 计费（元/百万 token）
  readonly supportsCustomModel?: boolean;       // 是否允许自定义 model 输入
}
```

### 7.3 OpenAI 兼容基类

国内主流大模型几乎都提供了 OpenAI 兼容端点，请求/响应/SSE 格式与 OpenAI `/v1/chat/completions` 一致，只需改 `base_url` + `api_key` + `model`。

`OpenAiCompatibleAdapter` 基类封装通用的 OpenAI 协议逻辑：

```ts
// openai-compatible.adapter.ts
@Injectable()
export abstract class OpenAiCompatibleAdapter implements LlmAdapter {
  abstract readonly name: string;
  abstract readonly defaultBaseUrl: string;
  abstract readonly models: string[];

  // 可选配置字段，子类按需覆写
  readonly paramBounds?: ParamBounds;
  readonly supportsTopP?: boolean;
  readonly extraBody?: (req: ChatRequest) => Record<string, any>;
  readonly keyHint?: string;
  readonly keyFormat?: string;
  readonly pricing?: Pricing;
  readonly supportsCustomModel?: boolean;

  buildRequest(req, apiKey) {
    // 构造 url = baseUrl + '/chat/completions'
    // headers = { Authorization: Bearer, Content-Type, Accept }
    // body = { model, messages, stream, temperature?, top_p?, max_tokens?, ...extraBody }
  }
  parseResponse(raw) { /* OpenAI 标准解析 */ }
  parseStreamChunk(chunkStr) { /* data: {...}\n\n + data: [DONE] */ }
}
```

### 7.4 厂商适配器示例（极简）

每个厂商适配器只声明差异配置，约 30 行：

```ts
// zhipu.adapter.ts
@Injectable()
export class ZhipuAdapter extends OpenAiCompatibleAdapter {
  readonly name = 'zhipu';
  readonly defaultBaseUrl = 'https://open.bigmodel.cn/api/paas/v4';
  readonly models = ['glm-4', 'glm-4-plus', 'glm-4-air', 'glm-4-flash', 'glm-5.1'];
  readonly paramBounds = {
    temperature: { min: 0, max: 0.99, default: 0.7 },
    top_p: { min: 0, max: 1, default: 1 },
    max_tokens: { min: 1, max: 128000, default: 1024 },
  };
  readonly keyHint = '在智谱开放平台获取，格式 {apiid}.{secret}';
  readonly pricing = { input: 50, output: 50, currency: 'CNY' };
}
```

### 7.5 9 家厂商适配明细

| 厂商 | base_url | 模型 | 特殊处理 |
|---|---|---|---|
| DeepSeek | `api.deepseek.com` | deepseek-chat、deepseek-reasoner | 基线 |
| 智谱 GLM | `open.bigmodel.cn/api/paas/v4` | glm-4/4-plus/4-air/4-flash/5.1 | Key 格式 `{id}.{secret}` |
| Kimi | `api.moonshot.cn/v1` | moonshot-v1-8k/32k/128k、kimi-k2.6 | 模型名带上下文长度 |
| 通义千问 | `dashscope.aliyuncs.com/compatible-mode/v1` | qwen-plus/max/turbo/3-max | 必须 compatible-mode 路径 |
| 文心一言 | `qianfan.baidubce.com/v2` | ernie-4.0-8k/3.5-8k/speed-8k/lite-8k | `supportsTopP=false`，Key 格式 `bce-v3-` |
| 星火 | `spark-api-open.xf-yun.com/v1` | spark-lite/pro/max/4.0-ultra | Key 是 APIPassword |
| 混元 | `api.hunyuan.cloud.tencent.com/v1` | hunyuan-turbos-latest/standard/pro/lite | `extraBody` 注入 `enable_enhancement` |
| 豆包 | `ark.cn-beijing.volces.com/api/v3` | doubao-1.5-pro-32k 等 | 直接模型名 |
| MiniMax | `api.minimaxi.com/v1` | abab6.5-chat、MiniMax-M2 | 无 |

### 7.6 注册表

`ProviderRegistry` 构造函数注入全部 9 个适配器并 register，`list()` 返回全部厂商信息（含 paramBounds / pricing 等给前端用），`get(name)` 按名取适配器。

```ts
@Injectable()
export class ProviderRegistry {
  private readonly adapters = new Map<string, LlmAdapter>();
  constructor(
    deepseek: DeepSeekAdapter,
    zhipu: ZhipuAdapter,
    kimi: KimiAdapter,
    // ... 共 9 个
  ) {
    this.register(deepseek);
    this.register(zhipu);
    // ...
  }
}
```

新增厂商只需：
1. 在 `adapters/` 新建一个适配器文件（继承基类，~30 行）
2. 在 `provider.registry.ts` 构造函数加 1 个注入 + 1 个 register
3. 在 `providers.module.ts` providers 数组加 1 行

前端下拉框自动出现该厂商，无需改前端代码。

---

## 8. 接口设计

### 8.1 后端 API 总览

所有接口前缀 `/api`，除 `/health` 外需 `Authorization: Bearer <token>`。

| 方法 | 路径 | 鉴权 | 说明 |
|---|---|---|---|
| GET | `/api/health` | 否 | 健康检查 |
| POST | `/api/auth/register` | 否 | 注册 |
| POST | `/api/auth/login` | 否 | 登录，返回 JWT |
| GET | `/api/auth/me` | 是 | 当前用户信息 |
| GET | `/api/providers` | 是 | 厂商列表（含 paramBounds / pricing / keyHint） |
| GET | `/api/providers/:id/models` | 是 | 指定厂商的模型列表 |
| GET | `/api/api-keys/:provider` | 是 | 读取已保存 Key（掩码返回） |
| PUT | `/api/api-keys/:provider` | 是 | 保存/更新 Key（加密入库） |
| DELETE | `/api/api-keys/:provider` | 是 | 删除 Key |
| POST | `/api/chat` | 是 | 非流式调用 |
| POST | `/api/chat/stream` | 是 | 流式调用（SSE） |
| POST | `/api/history` | 是 | 新建一条历史 |
| GET | `/api/history` | 是 | 当前用户历史列表（最近 100 条） |
| GET | `/api/history/:id` | 是 | 单条历史详情 |
| DELETE | `/api/history/:id` | 是 | 删除单条 |
| DELETE | `/api/history` | 是 | 清空本人历史 |

### 8.2 鉴权

- 登录返回 `{ accessToken, user }`，token 有效期 7 天
- 受保护接口需 `Authorization: Bearer <token>`
- 前端 axios 拦截器自动注入；401 时清 token 并跳登录页

### 8.3 统一数据结构

#### ChatRequest（前端 → 后端）

```json
{
  "provider": "zhipu",
  "baseUrl": "https://open.bigmodel.cn/api/paas/v4",
  "apiKey": "sk-xxx",
  "model": "glm-4",
  "messages": [
    { "role": "system", "content": "你是一个翻译助手" },
    { "role": "user", "content": "把下面的句子翻译成英文：你好" }
  ],
  "params": {
    "temperature": 0.7,
    "top_p": 1,
    "max_tokens": 1024,
    "stream": false
  }
}
```

> `apiKey` 可选：前端一次性传入则用之；未传则后端按 `userId + provider` 从数据库读取已加密的 Key。

#### ChatResponse（非流式）

```json
{
  "ok": true,
  "provider": "zhipu",
  "model": "glm-4",
  "content": "Hello",
  "raw": { /* 厂商返回的完整原始响应 */ },
  "usage": { "prompt_tokens": 12, "completion_tokens": 3, "total_tokens": 15 },
  "timing": { "firstTokenMs": 520, "totalMs": 612 }
}
```

#### 流式响应（SSE 事件流）

`Content-Type: text/event-stream`，每个事件 `data: <json>\n\n`：

```json
data: {"type":"delta","content":"你"}

data: {"type":"delta","content":"好"}

data: {"type":"done","usage":{...},"timing":{...},"raw":{...}}

data: {"type":"error","message":"Invalid API Key","status":401}
```

### 8.4 Providers 响应（含扩展字段）

`GET /api/providers` 返回：

```json
[
  {
    "name": "zhipu",
    "defaultBaseUrl": "https://open.bigmodel.cn/api/paas/v4",
    "models": ["glm-4", "glm-4-plus", "glm-4-air", "glm-4-flash", "glm-5.1"],
    "paramBounds": {
      "temperature": { "min": 0, "max": 0.99, "default": 0.7 },
      "top_p": { "min": 0, "max": 1, "default": 1 },
      "max_tokens": { "min": 1, "max": 128000, "default": 1024 }
    },
    "supportsTopP": true,
    "keyHint": "在智谱开放平台获取，格式 {apiid}.{secret}",
    "keyFormat": "^[a-zA-Z0-9]+\\.[a-zA-Z0-9]+$",
    "pricing": { "input": 50, "output": 50, "currency": "CNY" },
    "supportsCustomModel": false
  }
]
```

前端据此动态渲染参数面板与 Key 提示。

### 8.5 错误响应

非流式接口 HTTP 非 2xx：

```json
{
  "message": "temperature 越界：允许范围 0 ~ 2，当前 5",
  "error": "Bad Request",
  "statusCode": 400
}
```

流式接口在事件流中发 `type:"error"` 后关闭连接。

---

## 9. 数据库模型

### 9.1 Prisma Schema

```prisma
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model User {
  id        String   @id @default(cuid())
  email     String   @unique
  password  String                     // bcryptjs 哈希
  createdAt DateTime @default(now())
  apiKeys   ApiKey[]
  histories History[]
}

model ApiKey {
  id        String   @id @default(cuid())
  userId    String
  provider  String                     // 'deepseek' / 'zhipu' / ...
  cipher    String                     // AES-256-GCM 加密后的 Key
  iv        String                     // 初始化向量(base64)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([userId, provider])          // 每用户每厂商一个 Key
}

model History {
  id         String   @id @default(cuid())
  userId     String
  title      String                    // 取首条 user 消息前 30 字
  request    String                    // JSON: ChatRequest
  response   String                    // JSON: ChatResponse 或流式聚合
  durationMs Int
  createdAt  DateTime @default(now())
  user       User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId, createdAt])
}
```

### 9.2 设计要点

- `ApiKey` 表的 `(userId, provider)` 唯一索引天然支持每用户每厂商独立保存 Key
- `History.request` 存完整 ChatRequest JSON（含 provider/model），前端可按厂商筛选
- 全部按 `userId` 隔离，用户 A 看不到用户 B 的任何数据
- SQLite 零配置，文件型数据库；生产可换 PostgreSQL（改 `datasource.provider`）

---

## 10. 前端关键交互设计

### 10.1 页面布局（Playground 主页）

```
┌──────────────────────────────────────────────────────────────┐
│ 顶栏：[Logo] [历史] [用户邮箱·退出]                            │
├───────────────────────┬──────────────────────────────────────┤
│  左侧（配置 & 消息）    │  右侧（结果展示）                       │
│  ┌─────────────────┐  │  ┌────────────────────────────────┐ │
│  │ 厂商▼ 模型▼      │  │  │ [发送] [cURL] [请求JSON]       │ │
│  │ ☐自定义BaseURL   │  │  ├────────────────────────────────┤ │
│  │ API Key [💾]     │  │  │ Tab: [可视化] [原始 JSON]       │ │
│  │  (格式提示)       │  │  │                                │ │
│  ├─────────────────┤  │  │  AI 回答区 / 流式逐字打印       │ │
│  │ 参数面板          │  │  │                                │ │
│  │ temperature ─o─  │  │  │                                │ │
│  │ top_p ─o─        │  │  ├────────────────────────────────┤ │
│  │ max_tokens [1024]│  │  │ 首 token / 总耗时 / token /    │ │
│  │ ☐ stream 流式     │  │  │ 预估费用 ¥x / 厂商·模型       │ │
│  ├─────────────────┤  │  └────────────────────────────────┘ │
│  │ 消息编辑器       │  │                                    │
│  │ system ____      │  │                                    │
│  │ [user] ____      │  │                                    │
│  │ [+ 添加消息]     │  │                                    │
│  └─────────────────┘  │                                    │
└───────────────────────┴──────────────────────────────────────┘
```

### 10.2 关键交互

- **未登录**：访问任意页面跳 `/login`；提供「去注册」入口
- **厂商切换**：自动切换默认 BaseURL、模型下拉、参数默认值、Key 掩码
- **参数动态边界**：`temperature` / `top_p` / `max_tokens` 的 min/max/step 由厂商 `paramBounds` 决定
- **top_p 条件隐藏**：`supportsTopP === false`（文心一言）时隐藏该控件，后端也不传
- **Key 格式提示**：切换厂商时输入框下方显示该厂商 Key 格式（如 `bce-v3-xxx`）
- **流式开关**：开启后右侧逐字打印，关闭后等待完整返回
- **历史回填**：一键载入历史请求的全部参数 + 消息 + 响应，可重新查看或修改后重发
- **复制 cURL**：生成等价 `curl` 命令，可直接在终端粘贴执行
- **费用估算**：底部芯片根据 `usage` + `pricing` 计算并展示「预估费用 ¥x」

---

## 11. 安全设计

1. **密码**：bcryptjs 哈希（saltRounds=10），不存明文
2. **API Key 加密存储**：AES-256-GCM 加密后入库；解密仅在调用时内存中进行；不打印到日志、不在任何响应中返回明文（GET 接口只返回掩码 `sk-****xxxx`）
3. **JWT**：签发 accessToken；secret 从环境变量读；7 天有效；前端 401 自动登出
4. **多用户隔离**：所有历史 / Key 查询都按 `req.user.id` 过滤
5. **参数边界校验**：后端按 `paramBounds` 校验越界值，返回 400，不发给厂商
6. **SSRF 防护**：自定义 BaseURL 默认禁用（`ALLOW_CUSTOM_BASEURL=false`），开启时校验 https + 非内网
7. **请求体限制**：后端限制 ≤ 1MB
8. **CORS**：仅允许配置的 `FRONTEND_ORIGIN`
9. **日志脱敏**：后端日志中 `apiKey` 字段自动替换为 `***`

---

## 12. 验收标准

### 12.1 功能验收（已通过）

| 编号 | 用例 | 结果 |
|---|---|---|
| AC1 | 注册新用户 → 登录 | ✅ 登录成功跳转 Playground |
| AC2 | 错误邮箱/密码登录 | ✅ 提示凭证错误，不跳转 |
| AC3 | 未登录访问 /playground | ✅ 自动跳 /login |
| AC4 | 非流式发送一条 user 消息 | ✅ 返回完整回答，原始 JSON 可见 |
| AC5 | 开启 stream 流式 | ✅ 逐字打印，结束后展示 usage 与耗时 |
| AC6 | temperature 0.2 vs 1.0 对比 | ✅ 两次结果有差异 |
| AC7 | 三轮消息（system+user+assistant） | ✅ 多轮上下文正确接收 |
| AC8 | 填入错误 Key | ✅ 右侧显示清晰错误信息与状态码 |
| AC9 | 复制 cURL | ✅ 终端粘贴可复现请求 |
| AC10 | 历史记录回填 | ✅ 一键回填参数+消息+响应 |
| AC11 | 用户 A 数据对用户 B 不可见 | ✅ 按 userId 隔离 |
| AC12 | 退出重登 | ✅ Key 与历史仍存在（存数据库） |
| AC13 | 自定义 BaseURL | ✅ 兼容 OpenAI 协议的第三方服务可正常返回 |
| AC14 | 切换到智谱 GLM，填入 Key，发送消息 | ✅ 走 `open.bigmodel.cn/api/paas/v4`，返回 glm-4 回答 |
| AC15 | 切换到 Kimi，选 moonshot-v1-128k 模型 | ✅ 走 `api.moonshot.cn/v1`，正常返回 |
| AC16 | 切换到通义千问 | ✅ 走 `compatible-mode/v1` 路径 |
| AC17 | 切换到百度 ernie，Key 填 `bce-v3-xxx` | ✅ 走 `qianfan.baidubce.com/v2`，正常返回 |
| AC18 | 切换到豆包，model 用直接模型名 | ✅ 走 `ark.cn-beijing.volces.com/api/v3`，正常返回 |
| AC19 | 切换到混元，请求体自动带 `enable_enhancement: true` | ✅ 原始 JSON Tab 可见该字段被透传 |
| AC20 | 切换厂商时参数面板自动重置为该厂商默认值 | ✅ temperature 等滑块跳到默认位置 |
| AC21 | 厂商不支持 top_p 时，top_p 控件隐藏 | ✅ UI 不显示，后端也不传该参数 |
| AC22 | 参数越界（如 temperature=5） | ✅ 后端返回 400 + 清晰错误，不发给厂商 |
| AC23 | 每个厂商的 Key 独立保存 | ✅ 切换厂商时显示各自掩码，互不影响 |
| AC24 | 流式调用智谱/Kimi/通义 | ✅ 逐字推送正常，done 事件带 usage |
| AC25 | 费用估算展示 | ✅ 结果区底部显示「预估费用 ¥x」 |

### 12.2 非功能验收

| 编号 | 项 | 结果 |
|---|---|---|
| NF-AC1 | 启动 | ✅ `npm run dev` 同时启动前后端 |
| NF-AC2 | 兼容性 | ✅ Chrome/Edge 最新版正常 |
| NF-AC3 | 安全 | ✅ 日志与数据库无 Key 明文 |
| NF-AC4 | 可扩展 | ✅ 新增厂商 1 适配器文件 + 2 处注册 |
| NF-AC5 | 性能 | ✅ 流式首 token ≤ 5s |

### 12.3 实测数据

| 指标 | 实测值 |
|---|---|
| 非流式调用耗时 | ~592 ms（DeepSeek） |
| 流式首 token 耗时 | ~1076 ms |
| 流式总耗时 | ~1317 ms（28 tokens） |
| token 统计 | prompt 5 / completion 90 / total 95（准确） |
| 参数边界校验 | ✅ temperature=5 返回 400「越界：允许范围 0 ~ 2」 |
| 前端控制台错误 | 无 JS 错误 |

---

## 13. 开发与部署

### 13.1 启动方式

```bash
# 1. 进入项目根目录
cd AI作业3

# 2. 安装根依赖（concurrently）
npm install

# 3. 安装前后端依赖
npm run install:all

# 4. 配置环境变量
cp .env.example backend/.env
# 填入 JWT_SECRET、AES_KEY

# 5. 初始化数据库
npm run db:generate
npm run db:migrate

# 6. 启动（前后端并行）
npm run dev
# -> 前端 http://localhost:5173
# -> 后端 http://localhost:8787/api
```

浏览器访问 http://localhost:5173 即可使用。

### 13.2 环境变量

`backend/.env`：

| 变量 | 说明 | 默认 |
|---|---|---|
| JWT_SECRET | JWT 签名密钥 | dev-jwt-secret-... |
| AES_KEY | API Key 加密密钥（32 字节 hex） | 0123... |
| FRONTEND_ORIGIN | CORS 允许的前端地址 | http://localhost:5173 |
| ALLOW_CUSTOM_BASEURL | 是否允许自定义 BaseURL | false |
| PORT | 后端端口 | 8787 |

### 13.3 根 package.json scripts

```json
{
  "scripts": {
    "install:all": "npm --prefix frontend install && npm --prefix backend install",
    "dev": "concurrently -n fe,be -c blue,green \"npm:dev:fe\" \"npm:dev:be\"",
    "dev:fe": "npm --prefix frontend run dev",
    "dev:be": "npm --prefix backend run start:dev",
    "build": "npm --prefix frontend run build && npm --prefix backend run build",
    "db:migrate": "npm --prefix backend run prisma:migrate",
    "db:generate": "npm --prefix backend run prisma:generate"
  }
}
```

### 13.4 重置数据库

```bash
cd backend
rm prisma/dev.db            # 删库
npx prisma db push          # 重建
```

### 13.5 生产部署建议

1. 前端 `npm run build` 产物用 nginx 托管
2. 后端 `npm run build` 后 `node dist/main.js` 跑起，用 pm2 守护
3. 强制 HTTPS，修改 `JWT_SECRET` 和 `AES_KEY` 为强随机值
4. 数据库从 SQLite 换 PostgreSQL（改 `schema.prisma` 的 `datasource.provider`）
5. `FRONTEND_ORIGIN` 改成实际前端域名

---

## 附：相关文档

- [用户使用手册](用户使用手册.md) — 面向最终用户的操作指南
- [多厂商扩展需求文档](多厂商扩展需求文档.md) — 9 家厂商适配规格与决策记录
