# 多厂商扩展需求文档

> 版本：v1.1（已确认决策定稿）
> 日期：2026-09-10
> 关联文档：[设计文档.md](设计文档.md)、[使用文档.md](使用文档.md)
> 范围：在已完成的 DeepSeek 单厂商基础上，扩展支持国内常用大模型 API
>
> 决策确认（v1.1）：
> - 首批厂商：智谱 GLM、Kimi、通义千问、文心一言、星火、混元、豆包、MiniMax 共 8 家全做
> - 豆包 model：用直接模型名（`doubao-1.5-pro-32k` 等），不做 ep- 自定义输入
> - 费用估算：做（P1，每个适配器配 pricing）
> - API Key 格式校验：弱提示（placeholder 提示，不强校验）

---

## 目录

- [1. 背景与目标](#1-背景与目标)
- [2. 调研结论：国内主流大模型 OpenAI 兼容情况](#2-调研结论国内主流大模型-openai-兼容情况)
- [3. 适配策略](#3-适配策略)
- [4. 架构优化：OpenAICompatibleAdapter 基类](#4-架构优化openaicompatibleadapter-基类)
- [5. 各厂商适配明细](#5-各厂商适配明细)
- [6. 前端改动需求](#6-前端改动需求)
- [7. 后端改动需求](#7-后端改动需求)
- [8. 数据库与配置改动](#8-数据库与配置改动)
- [9. 新增功能点](#9-新增功能点)
- [10. 验收标准补充](#10-验收标准补充)
- [11. 优先级与阶段划分](#11-优先级与阶段划分)
- [12. 待确认事项](#12-待确认事项)
- [13. 参考资料](#13-参考资料)

---

## 1. 背景与目标

### 1.1 背景
当前工具箱已完整实现 DeepSeek 单厂商的全流程（注册/登录/Key 加密/非流式+流式/历史/多用户隔离）。架构上预留了 `adapters/` 扩展点，但实际只有一个 DeepSeek 适配器。

### 1.2 目标
扩展支持国内常用的大模型 API，让用户在同一界面下拉切换厂商、分别保存各自 Key、对比调用效果。

### 1.3 范围
- 首批扩展厂商：**智谱 GLM、月之暗面 Kimi、通义千问、文心一言（千帆）、讯飞星火、腾讯混元、火山方舟豆包、MiniMax**
- 可选扩展：百川、零一万物 Yi、阶跃星辰 Step
- 不在本期范围：视频/图像/语音/Embedding 等非 chat 接口、厂商原生非 OpenAI 协议（如星火 WebSocket + HMAC 签名）——统一走各家提供的 OpenAI 兼容端点

---

## 2. 调研结论：国内主流大模型 OpenAI 兼容情况

**核心结论：国内主流大模型几乎都提供了 OpenAI 兼容端点**，请求/响应/SSE 格式与 OpenAI `/v1/chat/completions` 一致，只需改 `base_url` + `api_key` + `model`。现有 DeepSeek 适配器可几乎原封不动复用。

| 厂商 | 兼容程度 | OpenAI 兼容 base_url | Key 格式 / 鉴权 | 备注 |
|---|---|---|---|---|
| DeepSeek（已支持） | 高度兼容 | `https://api.deepseek.com` | `sk-xxx` Bearer | 当前基线 |
| 智谱 GLM | 高度兼容 | `https://open.bigmodel.cn/api/paas/v4` | `xxx.yyy` Bearer | 模型 glm-4 / glm-4-plus / glm-5.1 |
| 月之暗面 Kimi | 高度兼容 | `https://api.moonshot.cn/v1` | `sk-xxx` Bearer | 模型 moonshot-v1-8k/32k/128k、kimi-k2.6 |
| 通义千问 | 兼容模式 | `https://dashscope.aliyuncs.com/compatible-mode/v1` | `sk-xxx` Bearer | 必须用 compatible-mode 路径；模型 qwen-plus / qwen-max |
| 文心一言（千帆 v2） | 兼容模式 | `https://qianfan.baidubce.com/v2` | `bce-v3-xxx` Bearer | 必须用 v2 端点；模型 ernie-4.0-8k / ernie-3.5-8k |
| 讯飞星火 | 兼容模式 | `https://spark-api-open.xf-yun.com/v1` | APIPassword Bearer | 原生是 WebSocket + HMAC，但有 OpenAI 兼容端点；X1 用 v2 路径 |
| 腾讯混元 | 高度兼容 | `https://api.hunyuan.cloud.tencent.com/v1` | `sk-xxx` Bearer | 注意与 SecretId/SecretKey 签名方式区分，用 API Key 方式 |
| 火山方舟豆包 | 高度兼容 | `https://ark.cn-beijing.volces.com/api/v3` | `sk-xxx` Bearer | 模型用 `ep-xxx`（推理接入点 ID）或 doubao-1.5-pro-32k |
| MiniMax | 高度兼容 | `https://api.minimaxi.com/v1` | `sk-xxx` Bearer | 模型 abab6.5-chat、MiniMax-M2 |
| 百川 | 高度兼容 | `https://api.baichuan-ai.com/v1` | `sk-xxx` Bearer | baichuan4 |
| 零一万物 Yi | 高度兼容 | `https://api.lingyiwanwu.com/v1` | `sk-xxx` Bearer | yi-large |
| 阶跃星辰 Step | 高度兼容 | `https://api.stepfun.com/v1` | `sk-xxx` Bearer | step 系列，不支持 reasoning summary |

### 2.1 关键差异点（适配时需注意）
1. **base_url 路径不同**：有的带 `/v1`、有的带 `/v2`、有的带 `/api/v3`、有的带 `/compatible-mode/v1`
2. **Key 格式不同**：DeepSeek/Kimi/智谱/混元 是 `sk-xxx`；百度是 `bce-v3-xxx`；智谱是 `id.secret` 格式；星火是 APIPassword（纯字符串）
3. **模型名命名规则不同**：豆包用 `ep-xxx` 推理接入点 ID（用户在火山方舟控制台创建），其余用模型名
4. **参数支持差异**：
   - 部分厂商不支持 `top_p`（如部分 ernie 模型）
   - `max_tokens` 上限不同（Kimi 128k、DeepSeek 1M、混元 4k 等）
   - 混元有自定义参数 `enable_enhancement`
   - 阶跃不支持 `reasoning_effort` / `reasoning_summary`
5. **流式 SSE 格式**：绝大多数完全一致（`data: {choices:[{delta:{content}}]}` + `data: [DONE]`）
6. **计费差异**：各家每百万 token 价格不同，部分有缓存命中折扣

---

## 3. 适配策略

### 3.1 策略：统一走 OpenAI 兼容端点
**不实现各家原生协议**（如星火 WebSocket + HMAC、混元 TC3 签名、文心 access_token 交换），统一使用各家提供的 OpenAI 兼容 HTTP 端点。

理由：
1. 已有的 DeepSeek 适配器基于 OpenAI 协议，可最大程度复用
2. 各家 OpenAI 兼容端点已覆盖 chat 场景，足够满足「接口调试工具箱」定位
3. 维护成本可控：避免为每家维护一套签名/鉴权代码
4. 用户文档统一：一套请求结构，差异仅在 baseURL/模型/参数边界

### 3.2 架构层级
```
LlmAdapter（接口，已有）
  └── OpenAiCompatibleAdapter（新增基类，封装通用 OpenAI 协议逻辑）
        ├── DeepSeekAdapter（已有，改为继承基类）
        ├── ZhipuAdapter（新增，仅配置差异）
        ├── KimiAdapter
        ├── QwenAdapter
        ├── ErnieAdapter
        ├── SparkAdapter
        ├── HunyuanAdapter
        ├── DoubaoAdapter
        ├── MinimaxAdapter
        └── ...（后续扩展）
```

新增一个厂商只需写一个极简的配置类（baseURL + 模型列表 + 参数边界 + 厂商特定覆写）。

---

## 4. 架构优化：OpenAICompatibleAdapter 基类

### 4.1 目标
把 DeepSeek 适配器里通用的 OpenAI 协议逻辑（buildRequest / parseResponse / parseStreamChunk）抽成基类，让各厂商适配器只声明差异配置，不复写代码。

### 4.2 基类设计（伪代码）
```ts
// adapters/openai-compatible.adapter.ts
export abstract class OpenAiCompatibleAdapter implements LlmAdapter {
  abstract readonly name: string;
  abstract readonly defaultBaseUrl: string;
  abstract readonly models: string[];
  // 可选：参数边界，用于前端校验
  paramBounds?: {
    temperature?: { min: number; max: number; default: number };
    top_p?: { min: number; max: number; default: number };
    max_tokens?: { min: number; max: number; default: number };
  };
  // 可选：是否支持 top_p（部分 ernie 不支持）
  supportsTopP = true;
  // 可选：额外请求体字段（如混元 enable_enhancement）
  extraBody?: (req: ChatRequest) => Record<string, any>;

  buildRequest(req: ChatRequest, apiKey: string) {
    const baseUrl = (req.baseUrl || this.defaultBaseUrl).replace(/\/$/, '');
    const url = `${baseUrl}/chat/completions`;
    const headers = {
      'Content-Type': 'application/json',
      Authorization: `Bearer ${apiKey}`,
      Accept: req.params.stream ? 'text/event-stream' : 'application/json',
    };
    const body: any = {
      model: req.model,
      messages: req.messages,
      stream: !!req.params.stream,
    };
    if (this.supportsTopP && req.params.top_p !== undefined) body.top_p = req.params.top_p;
    if (req.params.temperature !== undefined) body.temperature = req.params.temperature;
    if (req.params.max_tokens !== undefined) body.max_tokens = req.params.max_tokens;
    const extra = this.extraBody?.(req);
    if (extra) Object.assign(body, extra);
    return { url, headers, body };
  }

  parseResponse(raw: any) { /* 与现有 DeepSeek 一致 */ }
  parseStreamChunk(chunkStr: string) { /* 与现有 DeepSeek 一致 */ }
}
```

### 4.3 厂商适配器示例（极简）
```ts
// adapters/zhipu.adapter.ts
@Injectable()
export class ZhipuAdapter extends OpenAiCompatibleAdapter {
  readonly name = 'zhipu';
  readonly defaultBaseUrl = 'https://open.bigmodel.cn/api/paas/v4';
  readonly models = ['glm-4', 'glm-4-plus', 'glm-4-air', 'glm-5.1'];
  paramBounds = {
    temperature: { min: 0, max: 1, default: 0.7 },
    top_p: { min: 0, max: 1, default: 1 },
    max_tokens: { min: 1, max: 128000, default: 1024 },
  };
}
```

### 4.4 现有 DeepSeekAdapter 重构
让现有 DeepSeekAdapter 继承 `OpenAiCompatibleAdapter`，仅保留差异（默认 baseURL、模型列表、参数边界）。代码量从 ~100 行降到 ~15 行。

---

## 5. 各厂商适配明细

### 5.1 智谱 GLM（zhipu）
- base_url：`https://open.bigmodel.cn/api/paas/v4`
- 模型：`glm-4`、`glm-4-plus`、`glm-4-air`、`glm-4-flash`、`glm-5.1`
- Key 格式：`{apiid}.{secret}`（点号分隔，整体作为 Bearer token）
- 参数边界：temperature 0-0.99、top_p 0-1、max_tokens 上限 128000
- 特殊：无

### 5.2 月之暗面 Kimi（kimi）
- base_url：`https://api.moonshot.cn/v1`
- 模型：`moonshot-v1-8k`、`moonshot-v1-32k`、`moonshot-v1-128k`、`kimi-k2.6`
- Key 格式：`sk-xxx`
- 参数边界：temperature 0-1、top_p 0-1、max_tokens 取决于模型上下文（8k/32k/128k）
- 特殊：模型名带上下文长度，选择模型即决定上下文窗口

### 5.3 通义千问（qwen）
- base_url：`https://dashscope.aliyuncs.com/compatible-mode/v1`（**必须用 compatible-mode 路径**）
- 模型：`qwen-plus`、`qwen-max`、`qwen-turbo`、`qwen3-max`
- Key 格式：`sk-xxx`
- 参数边界：temperature 0-2、top_p 0-1、max_tokens 上限按模型
- 特殊：不用 compatible-mode 路径会走 DashScope 原生协议（不兼容），必须强约束

### 5.4 文心一言 / 百度千帆（ernie）
- base_url：`https://qianfan.baidubce.com/v2`（**必须用 v2 端点**）
- 模型：`ernie-4.0-8k`、`ernie-3.5-8k`、`ernie-speed-8k`、`ernie-lite-8k`
- Key 格式：`bce-v3-xxx`（前端需提示用户在千帆控制台创建 API Key，格式与 sk- 不同）
- 参数边界：部分模型不支持 `top_p`，temperature 0-1
- 特殊：免费模型 ernie-3.5-8k / ernie-speed-8k，适合用户测试

### 5.5 讯飞星火（spark）
- base_url：`https://spark-api-open.xf-yun.com/v1`（X1 用 `/v2`）
- 模型：`spark-lite`、`spark-pro`、`spark-max`、`spark-4.0-ultra`、`x1`
- Key 格式：APIPassword（在控制台「HTTP 服务接口认证信息」获取，纯字符串）
- 参数边界：temperature 0-1、top_p 0-1、max_tokens 按模型
- 特殊：X1 模型需走 `/v2` 路径（基类 baseUrl 可配置，或适配器内根据 model 判断）

### 5.6 腾讯混元（hunyuan）
- base_url：`https://api.hunyuan.cloud.tencent.com/v1`
- 模型：`hunyuan-turbos-latest`、`hunyuan-standard`、`hunyuan-pro`、`hunyuan-lite`
- Key 格式：`sk-xxx`（在混元控制台创建「API Key」，**不要用 SecretId/SecretKey 签名方式**）
- 参数边界：temperature 0-2、top_p 0-1、max_tokens 上限 4000
- 特殊：有自定义参数 `enable_enhancement`（默认 true），用 `extraBody` 注入；并发默认限制 5

### 5.7 火山方舟豆包（doubao）
- base_url：`https://ark.cn-beijing.volces.com/api/v3`
- 模型：`doubao-1.5-pro-32k-250115`、`doubao-1.5-lite-32k`、`doubao-pro-32k`、`doubao-pro-128k`（直接模型名，**不做 ep- 自定义输入**，已确认）
- Key 格式：`sk-xxx`
- 参数边界：temperature 0-2、top_p 0-1、max_tokens 按模型
- 特殊：无（决策确认用直接模型名，简化前端逻辑，不做 supportsCustomModel）

### 5.8 MiniMax（minimax）
- base_url：`https://api.minimaxi.com/v1`
- 模型：`abab6.5-chat`、`abab6.5s-chat`、`MiniMax-M2`
- Key 格式：`sk-xxx`
- 参数边界：temperature 0-2、top_p 0-1
- 特殊：无

### 5.9 可选扩展
- **百川**：`https://api.baichuan-ai.com/v1`，模型 `baichuan4`
- **零一万物 Yi**：`https://api.lingyiwanwu.com/v1`，模型 `yi-large`
- **阶跃星辰 Step**：`https://api.stepfun.com/v1`，模型 `step-1`、`step-3.7-flash`，不支持 reasoning summary

---

## 6. 前端改动需求

### 6.1 厂商选择组件（ProviderSelect）
- 下拉框展示全部已注册厂商（从 `/api/providers` 动态拉取，已有逻辑支持）
- 切换厂商时：
  - 自动填充默认 baseURL（已有）
  - 自动切换模型列表（已有）
  - **新增**：加载该厂商的参数边界（temperature/top_p/max_tokens 的 min/max/default）并应用到 ParamPanel
  - **新增**：若厂商不支持 top_p（如部分 ernie），隐藏该控件
  - **新增**：Key 输入框 placeholder 根据厂商显示格式提示（如百度提示 `bce-v3-xxx`，星火提示 APIPassword）

### 6.2 模型选择组件（ModelSelect）
- **新增「自定义输入」模式**：豆包用 `ep-xxx` 作为 model，用户需自己填。下拉框改为「可选下拉 + 可手动输入」的组合控件（MUI `Autocomplete` + `freeSolo`）
- 模型下拉项展示该厂商支持的模型列表

### 6.3 参数面板（ParamPanel）
- **动态参数边界**：从厂商适配器读取 min/max/default，应用到 slider 与 number 输入
- **条件隐藏**：`supportsTopP === false` 时隐藏 top_p 控件
- 切换厂商时重置参数到该厂商默认值

### 6.4 结果展示（ResultViewer）
- 底部芯片已有「provider / model」展示，无需大改
- **新增**：若启用费用估算（见 9.1），显示「预估费用 ¥xxx」

### 6.5 Key 管理提示
- 每个厂商的 Key 独立保存（已有 `apiKey.findUnique({userId, provider})` 支持）
- 切换厂商时自动加载该厂商已保存的 Key 掩码（已有逻辑）
- **新增**：保存 Key 时前端做格式弱校验（仅提示，不强制）：
  - DeepSeek/Kimi/智谱/混元/豆包/MiniMax/通义：建议 `sk-` 或符合厂商格式
  - 百度：建议 `bce-v3-`
  - 星火：纯字符串 APIPassword
- **新增**：Key 输入框旁加「获取方式」链接（指向各家控制台）

### 6.6 cURL 复制
- 已有逻辑根据 baseUrl + model + messages 生成 curl，无需大改
- **新增**：豆包模型是 `ep-xxx`，复制出的 cURL 要正确带上该 model 值（现有逻辑已支持）

---

## 7. 后端改动需求

### 7.1 适配器层重构
- 新增 `OpenAiCompatibleAdapter` 基类（见第 4 章）
- 重构 `DeepSeekAdapter` 继承基类
- 新增 8 个厂商适配器文件（每个 ~15 行）
- `ProviderRegistry` 构造函数注入全部新适配器并 register

### 7.2 适配器接口扩展
`LlmAdapter` 接口增加可选字段：
```ts
export interface LlmAdapter {
  // ...已有字段
  paramBounds?: { /* 参数边界 */ };
  supportsTopP?: boolean;
  extraBody?: (req: ChatRequest) => Record<string, any>;
  keyHint?: string;          // 前端 Key 输入框 placeholder 提示
  keyFormat?: string;        // Key 格式正则（弱校验）
  pricing?: {                // 计费（元/百万 token）
    input: number;
    inputCached?: number;
    output: number;
    currency?: string;
  };
  supportsCustomModel?: boolean; // 豆包为 true，允许用户自由填 ep-xxx
}
```

### 7.3 Providers 接口扩展
`GET /api/providers` 响应增加字段：
```json
{
  "name": "zhipu",
  "defaultBaseUrl": "https://open.bigmodel.cn/api/paas/v4",
  "models": ["glm-4", "glm-4-plus"],
  "paramBounds": {...},
  "supportsTopP": true,
  "keyHint": "在智谱开放平台获取，格式 id.secret",
  "pricing": {"input": 50, "output": 50, "currency": "CNY"},
  "supportsCustomModel": false
}
```

### 7.4 Chat 服务
- 已有 `resolveAdapter` / `resolveApiKey` / `validateBaseUrl` 逻辑通用，无需大改
- **新增**：请求前根据 `paramBounds` 做参数边界校验（拒绝越界值，避免厂商 400 错误）
- **新增**：根据 `supportsTopP` 决定是否传 top_p 给厂商
- **新增**：根据 `extraBody` 注入厂商自定义参数

### 7.5 ApiKey 模块
- 已有按 `userId + provider` 唯一存储，天然支持多厂商多 Key
- **无改动**

### 7.6 History 模块
- 已有按 userId 隔离 + 存 request/response
- **无改动**（历史里已含 provider/model，前端可按厂商筛选展示，可选优化）

---

## 8. 数据库与配置改动

### 8.1 Prisma Schema
**无需改动**。`ApiKey` 表已有 `(userId, provider)` 唯一索引，天然支持每用户每厂商一个 Key。`History` 表已存 request JSON（含 provider/model）。

### 8.2 环境变量
`backend/.env` 新增（可选）：
```
# 各厂商默认 pricing 是否展示（true/false）
SHOW_PRICING=true
```

### 8.3 ALLOW_CUSTOM_BASEURL
已有开关，保持默认 false。开启后用户可覆盖任何厂商的 baseURL（用于代理/私有部署）。无需改动。

---

## 9. 新增功能点

### 9.1 Token 费用估算（P1）
- 每个适配器声明 `pricing: { input, inputCached?, output, currency }`（单位：元/百万 token）
- 请求完成后根据 `usage.prompt_tokens` / `completion_tokens` 计算费用
- 前端结果区底部芯片新增「预估费用 ¥x.xxxx」
- 注意：仅估算，实际计费以厂商账单为准

### 9.2 参数边界动态校验（P0）
- 前端 ParamPanel 根据厂商 `paramBounds` 动态设置 slider 范围与默认值
- 后端请求前做边界校验，越界返回 400 + 清晰错误信息
- 切换厂商时自动重置参数到默认值

### 9.3 模型自定义输入（~~P0，豆包必需~~ → 已降级为 P2 可选）
- ~~豆包决策改为直接模型名，不再需要 ep- 自定义输入~~
- 前端 ModelSelect 保持现有下拉选择即可
- 若后续有厂商需要自定义 model，再改用 MUI `Autocomplete` + `freeSolo`

### 9.4 预设场景模板（P2，可选）
- 内置「翻译 / 摘要 / 代码生成 / 对话」模板，一键加载 system prompt + 示例消息
- 与厂商无关，纯前端静态配置

### 9.5 历史按厂商筛选（P2，可选）
- 历史页增加「按厂商筛选」下拉
- 前端本地过滤即可，无需后端改动

---

## 10. 验收标准补充

在原 13 项功能验收基础上新增：

| 编号 | 用例 | 预期结果 |
|---|---|---|
| AC14 | 切换到智谱 GLM，填入 Key，发送消息 | 走 `open.bigmodel.cn/api/paas/v4`，返回 glm-4 回答 |
| AC15 | 切换到 Kimi，选 moonshot-v1-128k 模型 | 走 `api.moonshot.cn/v1`，正常返回 |
| AC16 | 切换到通义千问，必须走 compatible-mode 路径 | 走 `dashscope.aliyuncs.com/compatible-mode/v1`，返回 qwen 回答 |
| AC17 | 切换到百度 ernie，Key 填 `bce-v3-xxx` | 走 `qianfan.baidubce.com/v2`，正常返回 |
| AC18 | 切换到豆包，model 用 `ep-xxx` 自定义输入 | 走 `ark.cn-beijing.volces.com/api/v3`，正常返回 |
| AC19 | 切换到混元，请求体自动带 `enable_enhancement: true` | 通过原始 JSON Tab 可见该字段被透传 |
| AC20 | 切换厂商时参数面板自动重置为该厂商默认值 | temperature 等滑块跳到默认位置 |
| AC21 | 厂商不支持 top_p 时，top_p 控件隐藏 | UI 不显示 top_p，后端也不传该参数 |
| AC22 | 参数越界（如 temperature=5） | 后端返回 400 + 清晰错误，不发给厂商 |
| AC23 | 每个厂商的 Key 独立保存 | 切换厂商时显示各自掩码，互不影响 |
| AC24 | 流式调用智谱/Kimi/通义 | 逐字推送正常，done 事件带 usage |
| AC25 | 费用估算展示 | 结果区底部显示「预估费用 ¥x.xxxx」 |

---

## 11. 优先级与阶段划分

### P0（必须，核心多厂商能力）
1. `OpenAiCompatibleAdapter` 基类抽取 + DeepSeek 重构
2. 新增 8 个厂商适配器（智谱、Kimi、通义、百度、星火、混元、豆包、MiniMax）
3. `LlmAdapter` 接口扩展（paramBounds / supportsTopP / extraBody / keyHint / supportsCustomModel）
4. `GET /api/providers` 响应扩展
5. 前端 ParamPanel 动态参数边界 + 条件隐藏 top_p
6. 前端 ModelSelect 支持自由输入（豆包 ep-xxx）
7. 前端 Key 输入框格式提示

### P1（建议，提升体验）
8. 后端参数边界校验
9. Token 费用估算（前端 + 后端 pricing 配置）
10. 流式调用全厂商验证

### P2（可选，锦上添花）
11. 预设场景模板
12. 历史按厂商筛选
13. 百川 / 零一万物 / 阶跃星辰 扩展厂商

### 工作量预估（不含编码细节）
- P0：后端适配器层重构 + 8 个适配器 + 接口扩展；前端 ParamPanel/ModelSelect/ProviderSelect 改造
- P1：参数校验 + pricing 配置 + 费用展示
- 每个新增厂商适配器约 15 行代码，整体改动集中在「基类抽取 + 接口扩展 + 前端动态参数」

---

## 12. 待确认事项

~~以下事项已全部确认（见文档头部 v1.1 决策）~~：

1. ~~首批厂商范围~~ → **已确认：8 家国内主流全做**（智谱、Kimi、通义、百度、星火、混元、豆包、MiniMax）
2. ~~豆包 model 自定义输入~~ → **已确认：用直接模型名**（`doubao-1.5-pro-32k` 等），不做 ep- 自定义输入
3. ~~费用估算~~ → **已确认：做（P1）**，每个适配器配 pricing
4. ~~Key 格式校验严格度~~ → **已确认：弱提示**（placeholder 提示格式，不强校验）

仍需实现时再定的小项（不阻塞开发）：
- 星火 X1 是否需单独 `/v2` 路径：默认全部走 `/v1`，X1 暂不支持
- 缓存命中折扣价：费用估算统一用未命中价（简化）
- 自定义 BaseURL 开关：保持默认 false（多厂商场景用户有代理需求时再开）
- 历史按厂商筛选：P2，可选

---

## 13. 参考资料

- [千帆大模型兼容 openai 接口](https://blog.csdn.net/m0_59167353/article/details/150440798)
- [零成本部署！手把手教你用百度千帆调用顶尖开源模型](https://blog.csdn.net/2601_96199011/article/details/161524055)
- [用 OpenAI SDK 能直接调用国产大模型吗？](https://cloud.tencent.com.cn/developer/article/2725085)
- [大模型 API 流式输出完全指南：OpenAI/Anthropic 接口范式与国内主流模型差异对比](https://blog.csdn.net/qq_31532979/article/details/163658529)
- [讯飞 http 协议通用鉴权 URL 生成说明](https://www.xfyun.cn/doc/spark/http_url_authentication.html)
- [星火大模型批处理 API 文档（含 OpenAI 兼容 Bearer 鉴权）](https://www.xfyun.cn/doc/spark/BatchAPI.html)
- [星火 X1 大模型体验（兼容 openAI）](https://blog.csdn.net/shy_snow/article/details/149705073)
- [腾讯混元快速入门](https://cloud.tencent.cn/document/product/1729/97730)
- [混元 OpenAI 兼容接口相关调用示例](https://cloud.tencent.com/document/product/1729/111007)
- [火山方舟 AI 加速网关模型调用示例代码](https://www.volcengine.com/docs/6559/2310290)
- [国产大模型 API 接入指南（Ai-Model-Gateway）](https://github.com/SSC-STUDIO/Ai-Model-Gateway/blob/main/docs/chinese-models-integration.md)
- [各 AI 大模型 base_url 与模型名速查（Winmail）](https://www.winmail.cn/docs/ai/llm/)
- [Claude Code 接入国产模型搭配说明](https://deepseek.csdn.net/6a073b1e662f9a54cb74d00f.html)
