# 大模型接口调用工具箱 · 用户使用手册

> 版本：v2.0（多厂商版）
> 日期：2026-09-10
> 适用对象：第一次使用本工具的最终用户
> 定位：大模型 API 的「专门定制版 Postman」

---

## 目录

- [1. 工具能做什么](#1-工具能做什么)
- [2. 30 秒快速上手](#2-30-秒快速上手)
- [3. 支持的厂商与模型](#3-支持的厂商与模型)
- [4. 如何获取 API Key](#4-如何获取-api-key)
- [5. 登录与注册](#5-登录与注册)
- [6. 主界面详解](#6-主界面详解)
- [7. 配置 API Key](#7-配置-api-key)
- [8. 选择厂商 / 模型 / BaseURL](#8-选择-厂商--模型--baseurl)
- [9. 调整参数](#9-调整参数)
- [10. 编辑消息](#10-编辑消息)
- [11. 发送请求与查看结果](#11-发送请求与查看结果)
- [12. 复制 cURL / 请求 JSON](#12-复制-curl--请求-json)
- [13. 历史记录](#13-历史记录)
- [14. 费用估算](#14-费用估算)
- [15. 常见问题](#15-常见问题)

---

## 1. 工具能做什么

**大模型接口调用工具箱** 是一个用于 **测试、调试、对比各家大模型 API** 的网页版工具。

核心能力：
- 多厂商切换：DeepSeek、智谱 GLM、Kimi、通义千问、文心一言、星火、混元、豆包、MiniMax 共 9 家
- 多模型对比：每家厂商内置多个模型，下拉切换即可
- 动态参数面板：temperature / top_p / max_tokens 的范围随厂商自动适配
- 流式 + 非流式：支持 SSE 逐字推送，也支持一次性返回
- API Key 加密存储：AES-256-GCM 加密入库，掩码回显，每个厂商独立保存
- 历史记录：按用户隔离，一键回填重发
- 费用估算：根据 token 用量与各厂商定价自动估算
- cURL 复制：一键生成等价 curl 命令，方便终端调试

---

## 2. 30 秒快速上手

1. 浏览器打开 **http://localhost:5173**
2. 用测试账号登录：邮箱 `test@example.com` / 密码 `123456`
3. 默认进入 DeepSeek 厂商 + `deepseek-chat` 模型
4. 在「消息列表」输入你想问的问题
5. 点右上角「**发送**」按钮
6. 右侧即可看到 AI 回答、token 用量、耗时

> 测试账号已预置 DeepSeek Key，可直接发送请求；用其他厂商需按第 4 章先保存对应 Key。

---

## 3. 支持的厂商与模型

工具内置 9 家国内主流大模型，全部走各家提供的 OpenAI 兼容端点：

| 厂商 | 接口地址 | 模型示例 | Key 格式 |
|---|---|---|---|
| DeepSeek | `api.deepseek.com` | deepseek-chat、deepseek-reasoner | `sk-xxx` |
| 智谱 GLM | `open.bigmodel.cn/api/paas/v4` | glm-4、glm-4-plus、glm-4-air、glm-4-flash、glm-5.1 | `{apiid}.{secret}` |
| Kimi | `api.moonshot.cn/v1` | moonshot-v1-8k/32k/128k、kimi-k2.6 | `sk-xxx` |
| 通义千问 | `dashscope.aliyuncs.com/compatible-mode/v1` | qwen-plus、qwen-max、qwen-turbo、qwen3-max | `sk-xxx` |
| 文心一言 | `qianfan.baidubce.com/v2` | ernie-4.0-8k、ernie-3.5-8k、ernie-speed-8k、ernie-lite-8k | `bce-v3-xxx` |
| 星火 | `spark-api-open.xf-yun.com/v1` | spark-lite、spark-pro、spark-max、spark-4.0-ultra | APIPassword |
| 混元 | `api.hunyuan.cloud.tencent.com/v1` | hunyuan-turbos-latest、hunyuan-standard、hunyuan-pro、hunyuan-lite | `sk-xxx` |
| 豆包 | `ark.cn-beijing.volces.com/api/v3` | doubao-1.5-pro-32k-250115、doubao-1.5-lite-32k、doubao-pro-32k、doubao-pro-128k | `sk-xxx` |
| MiniMax | `api.minimaxi.com/v1` | abab6.5-chat、abab6.5s-chat、MiniMax-M2 | `sk-xxx` |

> Kimi 的模型名带上下文长度（如 `moonshot-v1-128k`），选择模型即决定上下文窗口大小。
> 通义千问必须走 `compatible-mode` 路径，工具已自动处理，无需手动配置。
> 文心一言必须用千帆 v2 端点，工具已自动处理。

---

## 4. 如何获取 API Key

每家厂商的 Key 都需要到对应控制台申请。下面是各厂商申请入口：

| 厂商 | 申请地址 | 说明 |
|---|---|---|
| DeepSeek | https://platform.deepseek.com/api_keys | 登录后点「创建 API Key」，格式 `sk-xxx` |
| 智谱 GLM | https://open.bigmodel.cn/usercenter/apikeys | 创建后得到 `{apiid}.{secret}` 格式 |
| Kimi | https://platform.moonshot.cn/console/api-keys | 格式 `sk-xxx` |
| 通义千问 | https://dashscope.console.aliyun.com/apiKey | 阿里云账号，开通 DashScope，格式 `sk-xxx` |
| 文心一言 | https://console.bce.baidu.com/qianfan/ais/console/applicationConsole/application | 千帆控制台创建「API Key」，格式 `bce-v3-xxx` |
| 星火 | https://www.xfyun.cn/user-center/permit-information | 「HTTP 服务接口认证信息」里查 APIPassword |
| 混元 | https://console.cloud.tencent.com/hunyuan/api-key | 创建「API Key」，**勿用 SecretId/SecretKey 签名方式** |
| 豆包 | https://console.volcengine.com/ark/region:ark+cn-beijing/apiKey | 火山方舟控制台，格式 `sk-xxx` |
| MiniMax | https://platform.minimaxi.com/user-center/basic-information | 格式 `sk-xxx` |

**安全提示**：Key 相当于密码，请勿公开发布。工具对 Key 采用 AES-256-GCM 加密存储，调用时仅在内存解密，不写日志。

---

## 5. 登录与注册

### 5.1 登录

1. 打开 http://localhost:5173 会自动跳到 `/login`
2. 填邮箱、密码，点「登录」
3. 成功后跳转到 Playground 主页

**测试账号**（已预置 DeepSeek Key）：

| 项 | 值 |
|---|---|
| 邮箱 | `test@example.com` |
| 密码 | `123456` |

### 5.2 注册新账号

1. 登录页点底部「没有账号？去注册」
2. 填邮箱 + 密码（至少 6 位）+ 确认密码
3. 注册成功自动登录并跳转主页
4. 新账号需先保存对应厂商的 Key 才能调用

> 每个用户的 Key 和历史记录互相隔离，A 用户看不到 B 用户的任何数据。

### 5.3 退出登录

顶部栏右上角点用户邮箱 → 「退出登录」，跳回登录页。Token 会从浏览器清除，但数据库里的 Key 与历史仍在，下次登录可用。

---

## 6. 主界面详解

主界面（Playground）分为三大区域：

```
┌──────────────────────────────────────────────────┐
│ 顶部栏：Logo / 历史按钮 / 用户邮箱·退出          │
├──────────────┬───────────────────────────────────┤
│              │  右上角：发送 / cURL / 请求JSON   │
│  左侧配置区  ├───────────────────────────────────┤
│              │                                   │
│  · 厂商选择  │       右侧结果区                  │
│  · 模型选择  │                                   │
│  · BaseURL   │  · 可视化 Tab：AI 回答文本        │
│  · API Key   │  · 原始 JSON Tab：完整原始响应    │
│  · 参数面板  │  · 底部芯片：耗时/token/费用      │
│  · 消息列表  │                                   │
└──────────────┴───────────────────────────────────┘
```

---

## 7. 配置 API Key

每个厂商的 Key 独立保存，切换厂商时自动加载该厂商已保存的掩码。

### 7.1 保存 Key

1. 在左侧「API Key」输入框粘贴你的 Key
2. 点输入框右侧的 **保存图标（💾）**
3. 提示「已保存」，Key 以加密形式存入数据库，明文不再回显
4. 输入框只显示掩码，如 `sk-****7ef2`

### 7.2 Key 格式提示

切换到不同厂商时，输入框下方会自动显示该厂商的 Key 格式提示：
- DeepSeek / Kimi / 智谱 / 混元 / 豆包 / MiniMax / 通义：提示 `sk-xxx` 或对应格式
- 智谱：提示 `{apiid}.{secret}` 点号分隔
- 文心一言：提示 `bce-v3-xxx`
- 星火：提示 APIPassword（纯字符串）

> 工具只做格式弱提示，不强制校验。即使格式不完全匹配也能保存。

### 7.3 一次性使用

不保存也可以：直接在输入框填 Key，点发送即可。Key 仅用于本次请求，刷新页面即丢失。

### 7.4 切换厂商时

切换到另一个厂商时，Key 输入框会自动清空并加载该厂商已保存的掩码（如果有），互不干扰。

---

## 8. 选择厂商 / 模型 / BaseURL

### 8.1 选择厂商

左侧顶部「厂商」下拉框，列出全部 9 家厂商。切换时：
- 自动填充该厂商默认 BaseURL
- 自动切换到该厂商第一个模型
- **自动重置参数到该厂商默认值**（temperature / top_p / max_tokens）
- 自动加载该厂商已保存的 Key 掩码

### 8.2 选择模型

「模型」下拉框列出当前厂商支持的全部模型，选择即应用。

> Kimi 提示：模型名里的 `8k` / `32k` / `128k` 代表上下文窗口大小，按需选择。

### 8.3 自定义 BaseURL（可选）

勾选「自定义 BaseURL」可填自己的服务地址（如代理或私有部署）。默认关闭，需后端开启 `ALLOW_CUSTOM_BASEURL=true` 才能用。出于安全考虑，只允许 https 且不能指向内网。

---

## 9. 调整参数

左侧「参数」面板的参数范围会**根据所选厂商自动适配**：

### 9.1 temperature（温度）

- 范围随厂商不同：如智谱 0–0.99、文心 0–1、DeepSeek/豆包/MiniMax/通义/混元 0–2
- 越小越确定保守，越大越发散创意
- 滑块步进：范围 ≤1 时步进 0.01，范围 >1 时步进 0.1

### 9.2 top_p（核采样）

- 范围 0–1
- **文心一言不支持 top_p，切换到文心时该控件会自动隐藏**，后端也不会传该参数
- 其他厂商正常显示

### 9.3 max_tokens（最大生成长度）

- 上限随厂商不同：如 DeepSeek 8192、智谱 128000、豆包 32768、文心 4096、混元 4000
- 越界参数会在请求前被后端拦截，返回 400 + 清晰错误，不会发给厂商

### 9.4 stream（流式输出）

- 开关打开后，右侧逐字打印回答；关闭则等待完整返回
- 流式模式额外显示「首 token 耗时」

### 9.5 切换厂商时自动重置

每次切换厂商，参数会自动回到该厂商的默认值（由适配器配置），无需手动调整即可直接发送。

---

## 10. 编辑消息

左侧「消息列表」支持多轮对话编辑：

- **System Prompt**（顶部）：可选，填角色设定，如「你是一个翻译助手」
- **消息条目**：每条可选 role（system/user/assistant）+ 内容
- 每条右侧按钮：
  - **+** 在下方新增一条
  - **×** 删除本条
- 默认带一条 `user` 消息示例，可直接修改

多轮对话示例：
1. role=user: 「我喜欢吃苹果」
2. role=assistant: 「好的，了解了」
3. role=user: 「我刚才说我喜欢什么？」
→ AI 能基于上下文回答「苹果」

---

## 11. 发送请求与查看结果

### 11.1 发送

点右上角「**发送**」按钮：
- **非流式**：等待完整返回后一次性展示
- **流式**：右侧逐字打印，结束后显示首 token 耗时与总耗时

### 11.2 查看结果

右侧两个 Tab：

| Tab | 内容 |
|---|---|
| 可视化 | AI 回答的纯文本，支持换行 |
| 原始 JSON | 厂商返回的完整原始响应，调试必看 |

### 11.3 底部状态芯片

结果区底部显示：
- **首 token 耗时**：流式首字延迟
- **总耗时**：从发起到结束
- **prompt / completion / total tokens**：token 统计
- **预估费用 ¥x**：见第 14 章
- **厂商 / 模型**：当前请求使用的组合

### 11.4 错误处理

- Key 无效：返回 401/403，右侧显示厂商错误信息
- 参数越界：返回 400 + 清晰提示「temperature 越界：允许范围 0 ~ 2，当前 5」
- 未保存 Key：提示「未找到厂商 xxx 的 API Key，请先在设置中保存」

---

## 12. 复制 cURL / 请求 JSON

- **cURL** 按钮：复制等价 curl 命令到剪贴板，可在终端直接粘贴执行
- **请求 JSON** 按钮：复制本次请求体 JSON，方便贴到代码或文档里

生成的 cURL 会自动带上当前厂商的 BaseURL、模型、消息、参数，可直接复现请求。

---

## 13. 历史记录

### 13.1 自动保存

每次发送成功后自动保存到历史，按用户隔离。

### 13.2 查看历史

顶部栏点「历史」进入历史页，每条显示：
- 标题（取首条消息摘要）
- 厂商 / 模型
- 是否流式
- 耗时与 token 数
- 创建时间

### 13.3 回填重发

点某条历史的 **载入图标（↻）**，一键把该次的：
- 厂商 / 模型 / BaseURL
- System Prompt / 消息
- 参数 / 流式开关
- 原始响应

全部回填到 Playground，方便对比或重发。

### 13.4 删除

- 单条删除：点删除图标
- 一键清空：清空本人全部历史

---

## 14. 费用估算

结果区底部芯片会显示「预估费用 ¥x.xxxxxx」，按以下方式计算：

- 每个厂商在适配器里配置了 pricing（元/百万 token）
- 费用 = (prompt_tokens / 1,000,000) × 输入价 + (completion_tokens / 1,000,000) × 输出价
- 四舍五入到 6 位小数

部分厂商参考定价（元/百万 token）：

| 厂商 | 输入 | 输出 |
|---|---|---|
| DeepSeek | 1 | 2 |
| 智谱 GLM | 50 | 50 |
| Kimi | 12 | 12 |
| 通义千问 | 40 | 120 |
| 文心一言 | 120 | 120 |
| 星火 | 10 | 10 |
| 混元 | 15 | 50 |
| 豆包 | 5 | 5 |
| MiniMax | 30 | 30 |

> ⚠️ 仅估算，实际计费以厂商账单为准（部分厂商有缓存命中折扣、阶梯价等，估算用未命中价简化处理）。

---

## 15. 常见问题

### Q1：打开页面是空白？

确认前端服务已启动（终端应显示 `http://localhost:5173/`）。若没启动，在项目根目录执行 `npm run dev`。

### Q2：登录提示「凭证错误」？

- 确认邮箱密码无误
- 用测试账号 `test@example.com` / `123456` 验证
- 忘记密码可直接重置数据库（见 Q6）

### Q3：发送提示「未找到厂商 xxx 的 API Key」？

该厂商还没保存 Key。按第 4 章获取 Key，再按第 7 章保存。

### Q4：发送提示「temperature 越界」？

你填的参数超出该厂商允许范围。切换厂商时参数会自动重置到默认值，手动改完后请看错误提示里的允许范围。

### Q5：文心一言没有 top_p 控件？

文心部分模型不支持 top_p，工具自动隐藏该控件，后端也不会传该参数，属正常行为。

### Q6：如何彻底清除所有数据？

```bash
cd backend
del prisma\dev.db
npx prisma db push
```
这会删掉所有用户、Key、历史。浏览器 DevTools 里也可清掉 localStorage 中的 token。

### Q7：DeepSeek 返回的 model 是 `deepseek-flash`？

这是 DeepSeek 服务端的别名映射（`deepseek-chat` 调用后实际由 `deepseek-flash` 接待），不影响使用，请求与计费按你填的 `deepseek-chat` 走。

### Q8：流式输出看起来卡顿？

首 token 通常 1 秒左右，后续逐字推送。若整体很慢，通常是厂商服务端或网络问题，可在「原始 JSON」Tab 查看完整响应确认。

### Q9：能同时对比多家厂商吗？

可以。切换厂商分别发送请求，每次结果都会保存到历史。通过历史页可来回切换查看不同厂商对同一问题的回答。

### Q10：我的 Key 安全吗？

- Key 以 AES-256-GCM 加密存入数据库，不存明文
- 调用时仅在内存解密，不写日志
- 接口返回只显示掩码（如 `sk-****7ef2`），明文永不回显
- 每个用户的 Key 互相隔离，只能看到自己的

---

## 附：相关资源

- [设计文档](设计文档.md) — 架构与接口设计
- [多厂商扩展需求文档](多厂商扩展需求文档.md) — 扩展规格说明
- 测试账号：`test@example.com` / `123456`
- 前端地址：http://localhost:5173
- 后端地址：http://localhost:8787/api
